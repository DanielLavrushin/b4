"use strict";(globalThis.webpackChunkdocs=globalThis.webpackChunkdocs||[]).push([[3539],{9394(e,n,t){t.r(n),t.d(n,{assets:()=>d,contentTitle:()=>a,default:()=>h,frontMatter:()=>o,metadata:()=>s,toc:()=>l});const s=JSON.parse('{"id":"changelog","title":"Changelog","description":"[1.81.0] - 2026-09-06","source":"@site/docs/changelog.md","sourceDirName":".","slug":"/changelog","permalink":"/docs/changelog","draft":false,"unlisted":false,"editUrl":"https://github.com/DanielLavrushin/b4/tree/main/docs/docs/changelog.md","tags":[],"version":"current","sidebarPosition":20,"frontMatter":{"sidebar_position":20,"title":"Changelog"},"sidebar":"tutorialSidebar","previous":{"title":"How TSPU works","permalink":"/docs/advanced/tspu"}}');var i=t(2615),r=t(416);const o={sidebar_position:20,title:"Changelog"},a=void 0,d={},l=[{value:"[1.81.0] - 2026-09-06",id:"1810---2026-09-06",level:2},{value:"[1.80.4] - 2026-08-31",id:"1804---2026-08-31",level:2},{value:"[1.80.3] - 2026-08-30",id:"1803---2026-08-30",level:2},{value:"[1.80.2] - 2026-08-29",id:"1802---2026-08-29",level:2},{value:"[1.79.0] - 2026-08-18",id:"1790---2026-08-18",level:2},{value:"[1.78.0] - 2026-08-17",id:"1780---2026-08-17",level:2},{value:"[1.77.0] - 2026-08-15",id:"1770---2026-08-15",level:2},{value:"[1.76.3] - 2026-08-14",id:"1763---2026-08-14",level:2},{value:"[1.76.2] - 2026-08-12",id:"1762---2026-08-12",level:2},{value:"[1.76.1] - 2026-08-11",id:"1761---2026-08-11",level:2},{value:"[1.76.0] - 2026-08-10",id:"1760---2026-08-10",level:2},{value:"[1.75.0] - 2026-08-08",id:"1750---2026-08-08",level:2},{value:"[1.74.2] - 2026-08-02",id:"1742---2026-08-02",level:2},{value:"[1.74.1] - 2026-07-31",id:"1741---2026-07-31",level:2},{value:"[1.73.0] - 2026-07-05",id:"1730---2026-07-05",level:2},{value:"[1.72.0] - 2026-07-02",id:"1720---2026-07-02",level:2},{value:"[1.71.1] - 2026-06-29",id:"1711---2026-06-29",level:2},{value:"[1.71.0] - 2026-06-28",id:"1710---2026-06-28",level:2},{value:"[1.70.1] - 2026-06-23",id:"1701---2026-06-23",level:2},{value:"[1.70.0] - 2026-06-21",id:"1700---2026-06-21",level:2},{value:"[1.69.1] - 2026-06-14",id:"1691---2026-06-14",level:2},{value:"[1.69.0] - 2026-06-14",id:"1690---2026-06-14",level:2},{value:"[1.68.0] - 2026-06-10",id:"1680---2026-06-10",level:2},{value:"[1.67.2] - 2026-06-10",id:"1672---2026-06-10",level:2},{value:"[1.67.1] - 2026-06-10",id:"1671---2026-06-10",level:2},{value:"[1.67.0] - 2026-06-08",id:"1670---2026-06-08",level:2},{value:"[1.66.0] - 2026-06-07",id:"1660---2026-06-07",level:2},{value:"[1.65.0] - 2026-06-06",id:"1650---2026-06-06",level:2},{value:"[1.64.0] - 2026-06-01",id:"1640---2026-06-01",level:2},{value:"[1.63.0] - 2026-05-22",id:"1630---2026-05-22",level:2},{value:"[1.62.1] - 2026-05-11",id:"1621---2026-05-11",level:2},{value:"[1.61.4] - 2026-05-10",id:"1614---2026-05-10",level:2},{value:"[1.61.3] - 2026-05-09",id:"1613---2026-05-09",level:2},{value:"[1.60.1] - 2026-05-02",id:"1601---2026-05-02",level:2},{value:"[1.50.1] - 2026-04-29",id:"1501---2026-04-29",level:2},{value:"[1.50.0] - 2026-04-27",id:"1500---2026-04-27",level:2},{value:"[1.49.1] - 2026-04-20",id:"1491---2026-04-20",level:2},{value:"[1.49.0] - 2026-04-18",id:"1490---2026-04-18",level:2},{value:"[1.48.1] - 2026-04-05",id:"1481---2026-04-05",level:2},{value:"[1.48.0] - 2026-04-05",id:"1480---2026-04-05",level:2},{value:"[1.47.2] - 2026-04-01",id:"1472---2026-04-01",level:2},{value:"[1.47.0] - 2026-03-31",id:"1470---2026-03-31",level:2},{value:"[1.46.6] - 2026-03-26",id:"1466---2026-03-26",level:2},{value:"[1.46.5] - 2026-03-23",id:"1465---2026-03-23",level:2},{value:"[1.46.0] - 2026-03-22",id:"1460---2026-03-22",level:2},{value:"[1.45.1] - 2026-03-21",id:"1451---2026-03-21",level:2},{value:"[1.45.0] - 2026-03-21",id:"1450---2026-03-21",level:2},{value:"[1.44.1] - 2026-03-15",id:"1441---2026-03-15",level:2},{value:"[1.43.0] - 2026-03-14",id:"1430---2026-03-14",level:2},{value:"[1.42.1] - 2026-03-13",id:"1421---2026-03-13",level:2},{value:"[1.42.0] - 2026-03-11",id:"1420---2026-03-11",level:2},{value:"[1.41.0] - 2026-03-09",id:"1410---2026-03-09",level:2},{value:"[1.40.0] - 2026-03-08",id:"1400---2026-03-08",level:2},{value:"[1.39.1] - 2026-03-02",id:"1391---2026-03-02",level:2},{value:"[1.38.0] - 2026-02-27",id:"1380---2026-02-27",level:2},{value:"[1.37.0] - 2026-02-26",id:"1370---2026-02-26",level:2},{value:"[1.36.0] - 2026-02-16",id:"1360---2026-02-16",level:2},{value:"[1.35.2] - 2026-02-16",id:"1352---2026-02-16",level:2},{value:"[1.35.0] - 2026-02-15",id:"1350---2026-02-15",level:2},{value:"[1.34.0] - 2026-02-10",id:"1340---2026-02-10",level:2},{value:"[1.32.0] - 2026-01-19",id:"1320---2026-01-19",level:2},{value:"[1.31.2] - 2026-01-16",id:"1312---2026-01-16",level:2},{value:"[1.31.0] - 2026-01-11",id:"1310---2026-01-11",level:2},{value:"[1.30.1] - 2026-01-02",id:"1301---2026-01-02",level:2},{value:"[1.30.0] - 2026-01-02",id:"1300---2026-01-02",level:2},{value:"[1.29.1] - 2026-01-01",id:"1291---2026-01-01",level:2},{value:"[1.28.1] - 2025-12-30",id:"1281---2025-12-30",level:2},{value:"[1.27.2] - 2025-12-27",id:"1272---2025-12-27",level:2},{value:"[1.27.1] - 2025-12-27",id:"1271---2025-12-27",level:2},{value:"[1.27.0] - 2025-12-27",id:"1270---2025-12-27",level:2},{value:"[1.26.3] - 2025-12-26",id:"1263---2025-12-26",level:2},{value:"[1.26.2] - 2025-12-25",id:"1262---2025-12-25",level:2},{value:"[1.26.1] - 2025-12-24",id:"1261---2025-12-24",level:2},{value:"[1.25.4] - 2025-12-21",id:"1254---2025-12-21",level:2},{value:"[1.25.3] - 2025-12-21",id:"1253---2025-12-21",level:2},{value:"[1.25.2] - 2025-12-19",id:"1252---2025-12-19",level:2},{value:"[1.25.0] - 2025-12-19",id:"1250---2025-12-19",level:2},{value:"[1.24.0] - 2025-12-17",id:"1240---2025-12-17",level:2},{value:"[1.23.1] - 2025-12-16",id:"1231---2025-12-16",level:2},{value:"[1.23.0] - 2025-12-16",id:"1230---2025-12-16",level:2},{value:"[1.22.1] - 2025-12-09",id:"1221---2025-12-09",level:2},{value:"[1.22.0] - 2025-12-08",id:"1220---2025-12-08",level:2},{value:"[1.21.1] - 2025-12-07",id:"1211---2025-12-07",level:2},{value:"[1.21.0] - 2025-12-07",id:"1210---2025-12-07",level:2},{value:"[1.20.3] - 2025-12-03",id:"1203---2025-12-03",level:2},{value:"[1.20.1] - 2025-12-03",id:"1201---2025-12-03",level:2},{value:"[1.20.0] - 2025-12-03",id:"1200---2025-12-03",level:2},{value:"[1.19.1] - 2025-12-01",id:"1191---2025-12-01",level:2},{value:"[1.19.0] - 2025-11-29",id:"1190---2025-11-29",level:2},{value:"[1.18.5] - 2025-11-27",id:"1185---2025-11-27",level:2},{value:"[1.18.4] - 2025-11-25",id:"1184---2025-11-25",level:2},{value:"[1.18.3] - 2025-11-25",id:"1183---2025-11-25",level:2},{value:"[1.18.2] - 2025-11-25",id:"1182---2025-11-25",level:2},{value:"[1.18.1] - 2025-11-25",id:"1181---2025-11-25",level:2},{value:"[1.18.0] - 2025-11-24",id:"1180---2025-11-24",level:2},{value:"[1.17.1] - 2025-11-23",id:"1171---2025-11-23",level:2},{value:"[1.16.1] - 2025-11-20",id:"1161---2025-11-20",level:2},{value:"[1.16.0] - 2025-11-17",id:"1160---2025-11-17",level:2},{value:"[1.15.0] - 2025-11-16",id:"1150---2025-11-16",level:2},{value:"[1.14.0] - 2025-11-13",id:"1140---2025-11-13",level:2},{value:"[1.13.0] - 2025-11-10",id:"1130---2025-11-10",level:2},{value:"[1.12.0] - 2025-11-09",id:"1120---2025-11-09",level:2},{value:"[1.11.0] - 2025-11-05",id:"1110---2025-11-05",level:2},{value:"[1.10.1] - 2025-11-03",id:"1101---2025-11-03",level:2},{value:"[1.10.0] - 2025-11-02",id:"1100---2025-11-02",level:2},{value:"[1.9.2] - 2025-11-02",id:"192---2025-11-02",level:2},{value:"[1.9.1] - 2025-11-02",id:"191---2025-11-02",level:2},{value:"[1.9.0] - 2025-11-02",id:"190---2025-11-02",level:2},{value:"[1.8.0] - 2025-11-01",id:"180---2025-11-01",level:2},{value:"[1.7.0] - 2025-10-31",id:"170---2025-10-31",level:2},{value:"[1.6.0] - 2025-10-29",id:"160---2025-10-29",level:2},{value:"[1.5.0] - 2025-10-28",id:"150---2025-10-28",level:2}];function c(e){const n={a:"a",code:"code",em:"em",h2:"h2",li:"li",p:"p",strong:"strong",ul:"ul",...(0,r.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.h2,{id:"1810---2026-09-06",children:"[1.81.0] - 2026-09-06"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"The DPI Detector page is rebuilt around the sites typed into it, and every site is fetched twice"})," - once directly and once through b4, so a row says whether the ISP blocks the site, whether b4 fixes it, or whether it is still blocked, with a verdict sentence on top, a button that hands the still-blocked sites to Discovery, and a report to copy into an issue. The old page fetched only through the running engine, so a well-configured b4 hid the very blocking it was meant to show. The check takes any list of domains or URLs, can be filled from the sets, and its four scopes are named by the question they answer: Sites, DNS, Hosting and CDN, Telegram. The DNS check merges the two old ones into one table per provider over UDP, DoH and DoT, judges honesty against encrypted answers and names who really answered a port 53 query; the hosting check groups targets by network with the drop point and any whitelisted SNI that gets through; the Telegram check measures upload as well as download. The target lists are refreshed from the dpi-detector project as of 2026-09-04, several dead sites and addresses are dropped, and a button fetches the current upstream lists without a release."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"The Discovery page is rebuilt around the sites typed into it"})," - results and history list the sites in the order given, with one card and a plain-words verdict per site, and a found strategy is described in a sentence and shown with the same split, fake, target and DNS rows as the Sets page before it is applied. A run shows its phase, the number of configurations tested and the live log line instead of a percentage, Stop keeps what was found and marks it unconfirmed, the options are named by what they do, and the log of the last run stays available after a restart and can be downloaded."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Discovery looks for addresses the block does not cover, and can fix a site by DNS alone"})," - when a site's known addresses are unreachable or refuse the TLS handshake, the run asks a public resolver how clients in some five hundred other regions are answered, checks which of those addresses connect and complete a handshake for the site, and pins the site to them in the set it builds, with dead-address detection switched on so a pin that stops answering is skipped. A site that loads through an honest DNS answer or through such an address is reported as found with a set that changes no packets, where before it read as needing no bypass and got no set at all."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Discovery could apply a set that did not match what it had found, and a stopped run left the page stale until it was reloaded"})," - stopping halted the polling before the run had written its history, an alternate preset carried the geo categories of every other site in the run and the DNS redirect if any of them looked poisoned, the history applied the fastest entry's set to every site of a technique family under the family's name, and a site that was never blocked read as found while the remembered winners were tried ahead of the plain fetch."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A proxy set that matched every address cut the device it was bound to off the network, and taking the address back out did not bring it back"})," - the diversion rules pick a packet by its destination alone, so the device's DNS queries to the router, its DHCP renewals and its visits to the web interface went to the upstream proxy as well, a set without a device filter marked the router's own answers to a matched client for the proxy in turn, and an address removed from a set's targets stayed in the kernel set until the set was disabled or the service restarted. ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/322",children:"#322"})]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A set routed through an interface sent only the first packet of each connection that way, and every packet after it by the ordinary uplink"})," - the routing decision is carried on the connection mark, and b4 judges whether a router keeps that mark by looking for its own claim in the connection table, where a connection still in its handshake carries the claim on every router, including the ones whose firmware owns the mark and overwrites it a moment later; the same claim gated the source rewrite into the interface, so that was left out as well."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"On a router that downloads through wget, an update started from the web interface left grep's usage text in the update log, with the installer's lines stamped hours apart from b4's own"})," - the installer asks grep whether wget's help lists a timeout option by passing the option's name, which grep took for one of its own, and the two halves of the log took their time from different clocks."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1804---2026-08-31",children:"[1.80.4] - 2026-08-31"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Changing anything on a routing set took it apart before putting it back, so for about a second the traffic it matched left by the ordinary uplink"})," - the policy rule, the route, the prerouting jump, the chain and the addresses learned from DNS all came down before the replacement went in, and a set marks a connection only on its first packet, so a connection opened in that window stayed off the set for as long as it lived."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Every setting changed through the MCP server left a whole copy of the configuration behind, so memory climbed by tens of megabytes and stayed there until b4 was restarted"})," - the undo history keeps the configuration as it stood before each of the last twenty writes, and on a router with a large geosite set each of those copies carried a few megabytes of expanded domain list."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A proxy set that resolves its upstream by domain went back to sending bare addresses a few minutes after any settings change, and stayed that way until b4 was restarted"})," - the transparent proxy kept reading the domain index built at start-up, which the capture engine stops feeding once the configuration is applied again, so the addresses it had learned aged out with nothing to replace them. ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/338",children:"#338"})]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"On Keenetic, the proxy and mtproto-ws routing modes were reported as unavailable after every reboot, although the firmware ships the TPROXY and socket modules for its kernel"})," - b4 asked ",(0,i.jsx)(n.code,{children:"modprobe"})," to load them, which needs a module index the read-only NDMS firmware does not carry, so the request failed and the check that followed read the modules as absent; the installer's start script has a direct ",(0,i.jsx)(n.code,{children:"insmod"})," fallback, but those four modules were never on its list. ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/342",children:"#342"})]}),"\n"]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1803---2026-08-30",children:"[1.80.3] - 2026-08-30"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsxs)(n.strong,{children:["A routing table named in ",(0,i.jsx)(n.code,{children:"/etc/iproute2/rt_tables"})," was not recognised as the one a set was using"]})," - ",(0,i.jsx)(n.code,{children:"ip"})," prints such a table by its name rather than its number, which is how ASUS firmware ships tables 100 and 200, so b4 read its own live policy rule as missing and said so on every pass, the TUN engine left its rules behind on that table when it shut down, and b4 looked for those names in one file while ",(0,i.jsx)(n.code,{children:"ip"})," also reads the copy that ships with the package."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'A set in proxy or mtproto-ws mode stopped diverting anything on port 443, so Telegram sat at "Connecting" while the same set still worked on its other ports'})," - the per-set prerouting jump moved below the capture engine's own chain in 1.80.0, and NFQUEUE ends the chain, so the reply packets of a diverted connection were taken before the set's socket rule could hand them to its listener; on nftables the two chains carry priorities that settle this, iptables has no priorities and nothing held the order."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A set routed through an interface built its address list, its marks and its table but never got the rule that sends the mark to that table, so everything it matched left by the ordinary uplink"})," - the rule goes in and the shapes older releases wrote are swept after it, one of those shapes carries no mask, which reaches the kernel as a request to match on the mark value alone, so every kernel up to 4.17, which is what Keenetic, Padavan and the older OpenWrt and Merlin builds run, took the rule that had just gone in, and nothing ever read the policy rules back to notice."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A domain in a proxy-mode set failed when reached through b4's built-in SOCKS5 server, while the same domain worked for a client passing through"})," - traffic the router originates is marked by the set's own output chain and comes back through prerouting over the local route, where the guard added in 1.80.0 returns any packet that already carries a routing mark, including the set's own."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsxs)(n.strong,{children:["A set in proxy mode with a hand-set ",(0,i.jsx)(n.code,{children:"fwmark"})," took the router's whole network down"]})," - the interface path checks such a value and assigns its own where it cannot be carried, the proxy path took it as given, and a value with bits outside the range b4 masks on leaves a policy rule the kernel reads as matching every packet that carries no routing mark at all, so all of it went into the set's local delivery table."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1802---2026-08-29",children:"[1.80.2] - 2026-08-29"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The interface reported an older version than the binary on disk, with nothing to say why"})," - the version is stamped in at build time, so a service whose file is replaced without a restart keeps answering with the version it was compiled as while the command line reports the new one, and where the running process cannot read its own path, as in a container without ",(0,i.jsx)(n.code,{children:"/proc"}),", the diagnostics named that path ",(0,i.jsx)(n.code,{children:"."})," instead of saying it did not know."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A set routed to a tunnel or an upstream proxy could take the router down within seconds of the service starting"})," - the rules pick a packet by its destination alone, so a packet the proxy handed back for that same address was picked again and sent straight back to it, thirty-one turns before its hop count ran out. Where such a set carried the router's own traffic as well, the proxy answered each turn by opening a connection of its own from the same box, on a fresh source port every time, until the memory was gone."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A routed set could carry nothing at all, with nothing in the interface saying otherwise"})," - the kernel's reverse-path check dropped every reply arriving on the set's interface, a set left on interface mode with no interface chosen or on proxy mode with no upstream port was dropped from the pass without a line in the log, turning domain-only matching off never rebuilt the set so its address list stayed empty, the firmware rebuilding its own firewall took b4's jump with it, and on a Broadcom router the firmware hands out the connection mark itself and overwrote the routing decision b4 keeps there."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A routed set could send half a connection one way and half the other, or claim what another program was using"})," - a domain's address is only learned when its lookup is answered, which is a moment after the connection has already started, so the handshake left by the ordinary uplink and the rest went down the set's route; the table number came from the output interface's name with nothing checking ",(0,i.jsx)(n.code,{children:"/etc/iproute2/rt_tables"})," or the rules already pointing at it; each proxy set put its firewall jump ahead of the set before it, so the last in the list claimed addresses two of them targeted; a filter written to match a LAN client can never match a packet the router itself crafts, so a device-bound set's fakes travelled the ordinary uplink; on nftables a proxy set's mark returns sat at the top of a shared hook chain and ended it for every set below; and every rebuild deleted the set's own policy rule before putting the identical one back, so anything marked in that window found nothing pointing at the set's table and took the main one."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A kill switch for a set's output interface"})," - when the interface went away the kernel removed the route b4 had put in the set's table, the mark rule found the table empty, and the lookup fell through to the main table, so the set left through the ordinary uplink with the router's real address while its rules still read as correct."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"A set routed into a tunnel no longer has its packets faked, fragmented and desynced"})," - the work was done on the inner packet, which is wrapped or terminated on the router before anything on the network sees it, so it bought nothing and cost CPU on every connection, and the fake packets were delivered to the tunnel program's own stack one hop away with no room to expire. SYN health checks, dead-IP escalation, IP block detection and TCP duplication stop with it; a set keeps its bypass strategy when it routes to a plain second uplink and when its output interface is present but down."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"b4's own firewall work could take the whole network with it"})," - on a router whose iptables has no lock flag the commands run unserialised while the firmware rewrites the same tables on WAN checks and DHCP events, so a command that lost that race left b4 on half a rule set and one start was fine while the next took the network off the air until a reboot; a running but overloaded b4 had its queue filled and the kernel dropped every packet past the four thousandth with nothing to say why; and stopping b4 put back a strict TCP window setting that this firewall drops packets over, so a page loaded its first fifty kilobytes and stopped."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Picking network interfaces to monitor could switch the bypass off for the whole network with nothing saying so"})," - the setting matches the interface a packet leaves by, which a VPN client or a transparent proxy moves without touching the list, so a selection made before that quietly stopped matching and every packet was queued to b4 and then accepted unchanged. A new ",(0,i.jsx)(n.a,{href:"https://docs.b4core.app/docs/guides",children:"Guides"})," section covers the three interface settings and running b4 alongside Xray."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"On the TUN engine b4 captured the wrong traffic and missed what it needed"})," - the capture rule sat below the routing rules a transparent proxy, a multi-WAN manager or a VPN script installs, so the network's packets were claimed before b4 saw them while what b4 did capture was the proxy's own upstream connection; a request from the LAN to a device on another bridge was carried into the tunnel and sent to the internet with the uplink's address; and only DNS queries were carried in, never the answers, so nothing learned an address into a routing set, stripped AAAA records or healed a dead address."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Telegram kept reporting the MTProto proxy as incorrectly configured and switching it off"})," - the dialog comes from a single four-byte -444 relayed back from a data center, not from a slow dial, and b4 could send an ordinary session to a data center's media edge, which answers a non-media session with exactly that; the 1.78.0 fix routed around nothing because both names lead to the same address. On a router without IPv6 the bridge also gave up on an edge whose name carries an IPv6 address, because a connection to one on a box with no IPv6 route is refused outright."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"An MCP server that can act on b4 rather than only describe it"})," - the AI could read status and change one setting at a time, so anything a user asked for came back as instructions to carry out by hand. It gains per-setting facts, geosite and geoip search, target and set editing, the last update's transcript, and behind a separate ",(0,i.jsx)(n.strong,{children:"Allow active probes"})," permission, testing whether a domain loads and running Discovery; every tool call and every turned-away request is logged. ",(0,i.jsx)(n.code,{children:"Settings > Integrations"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A Discovery run's result became unreachable the moment the run ended, and the watchdog judged domains on a fetch that skipped the bypass"})," - only the copy held in memory was consulted and that is dropped 30 seconds after a run finishes, a strategy named while a run was still testing read as settled, stopping a run early threw away what it had found, the history file grew by roughly half a megabyte per domain because every strategy tried was saved with a full copy of the set built to test it, and the watchdog's own checks carried the mark b4 puts on traffic it has already handled."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The settings folder was open for any account on the router to write to, and a downloaded backup could hold nothing"})," - the settings file holds the web interface login and the MCP access token yet was created readable and writable by everyone, the copies kept before each upgrade the same, and restoring followed folder shortcuts left in that folder so an entry in the archive could land anywhere; b4 also skipped every file marked as a program, and on drives prepared from Windows the settings files carry that mark."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Importing a zapret or byedpi configuration converted a fraction of what was pasted, and misdescribed what it did convert"})," - a configuration file was read one line at a time, so multi-line values, variable references and the order its launcher joins them in were all lost."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The MTProto settings page asked for a WEB proxy relay hostname while the proxy that serves it was off"})," - the WEB carrier reuses the MTProto proxy's listener and its secrets, so with that proxy off the card was collecting a hostname for a relay that could not answer, beside a secrets card whose every control was dead, under header chips repeating the switch, the hostname and the transport mode already visible below them."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Documentation for the Telegram WEB proxy, which shipped in 1.79.0 with nothing written about it"})," - its prerequisites cannot be worked out from the interface: the MTProto proxy has to be running, the relay needs a hostname of its own with publicly trusted TLS, and the link Telegram Desktop accepts carries a different form of the secret than the settings show. It joins a ",(0,i.jsx)(n.a,{href:"https://docs.b4core.app/docs/mtproto",children:"Telegram"})," section covering the three modes separately, replacing one page that described two of them and quoted log lines b4 does not write."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Update mirrors, and a personal Cloudflare Worker to put in them"})," - under Settings, Control, the Update mirrors field takes https addresses that the service and the installer try ahead of the built-in ones when GitHub cannot be reached, and b4 hands the list to the installer when an update starts. The documentation carries a Worker script that stands in for GitHub on a free Cloudflare account, so a reader blocked from the release host can mirror through one of their own."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Installing an update from a file, for a router that can reach no download source at all"})," - the update window takes a ",(0,i.jsx)(n.code,{children:"b4-linux-<arch>.tar.gz"})," fetched on another machine, with an optional field for the SHA256 from the release page, which is the one check the automatic path cannot make independently because it reads the archive and its checksum from the same host. The upload is refused unless it holds a b4 built for that router, so the wrong architecture is named rather than installed and rolled back."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Installing or updating the service failed where the network blocks GitHub's download hosts, and the update window could come up with no versions listed"})," - the release archive is handed over by a redirect to a separate GitHub CDN that several providers drop by address, and the fallback proxy passed that redirect back to the router instead of following it, so the router went to the blocked host anyway and waited out a two-minute timeout, while the version list was fetched by the browser against an hourly GitHub limit shared by every user behind the same address."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1790---2026-08-18",children:"[1.79.0] - 2026-08-18"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A WEB carrier for the MTProto proxy, which Telegram Desktop 7.1.1 reaches over ordinary HTTPS"})," - a client on a network that blocks MTProxy and fake-TLS outright had no way in, because every transport b4 offered still opened a recognisable MTProto socket from the client. b4 answers a relay hostname with a bridge page the client loads in a hidden WebView, multiplexes the MTProxy stream through a same-origin WebSocket, and hands each logical stream to the same secrets, DC pool and upstream routing as a plain connection; requests without a valid bridge token get an ordinary page. ",(0,i.jsx)(n.code,{children:"Settings > MTProto Proxy"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A set's DNS redirect answered SERVFAIL for every domain it covered as soon as its DoH server went unreachable"})," - the redirect had no fallback of any kind, so a resolver the censor happened to be blocking took the whole set offline while the bypass strategy underneath it was working perfectly well. b4 replays the last good answer or hands the query back to the resolver the client was already using, and a Fail closed switch under ",(0,i.jsx)(n.code,{children:"Sets > Routing > DNS Redirect"})," keeps the old behaviour."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Discovery offered a bypass strategy for domains that were never blocked"})," - nothing in a run was compared against its own no-bypass fetch, so one blocked domain put a strategy on the whole batch, and the improvement percentage measured the site against yandex.ru, the reference domain b4 uses to gauge the line. Each domain is judged against its own unmodified fetch and reported on its own."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A Discovery strategy was declared working on one page fetch that may not have succeeded at all"})," - only HTTP 451 counted as a failure, so a 502 longer than a kilobyte scored as a success, the probe negotiated HTTP/1.1 with no ALPN while every browser asks for HTTP/2, and a redirected page was scored over the address of the domain that redirected it. The winner is fetched three more times in the shape saving it as a set gives it, and one that fails any of those tries gives way to the next candidate."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"On a router with working IPv6, Discovery stopped after about twenty tests and reported that no bypass was needed"})," - its test connections were free to use either version of the protocol while b4 acts on IPv4 alone, so the opening test succeeded over a route nothing was interfering with."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Discovery built sets around a DNS redirect and geo categories the router had no use for"})," - the redirect was stamped onto every set of a run as soon as one domain looked poisoned, though the probes never used it themselves, and geosite and geoip categories were carried in whatever was installed, so a result for a CDN domain could not be added at all without a geo database."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Restarting or stopping the service from the interface went wrong on systemd"})," - b4 waited on a systemctl that systemd stops together with the service it is restarting, so every restart left ",(0,i.jsx)(n.code,{children:"Restart command failed: signal: terminated"})," in the error log, and an unfinished browser connection could hold a stop open for the full ten seconds until the kill landed while the firewall cleanup was still running."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"An update could stop the service loading, or leave a second b4 running outside the service manager"})," - the updater recognised every init system except systemd, so a unit written by anything other than b4's installer was rebuilt as a SysV init script, and it waited one second for the service to come back before starting b4 by hand, racing the manager's own restart."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A source allowlist for the SOCKS5 proxy, holding the IP addresses and CIDR ranges permitted to connect"})," - the only control over who reached the proxy was a username and password, which Chrome and Chromium cannot send at all, so a LAN client on either browser meant leaving the proxy open to everything that could reach its port. ",(0,i.jsx)(n.code,{children:"Settings > SOCKS5 Proxy"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The SOCKS5 proxy served clients it was meant to keep out"})," - a username without a password, or a password without a username, switched authentication off rather than failing, and the UDP relay took an address written into the associate request over the source of the control connection, so an association could be pointed at another host on the network."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Turning the SOCKS5 proxy off left the connections it was already carrying running"})," - only the listener closed, so established tunnels kept relaying until the client dropped them by itself, and changing the port or the bind address behaved the same way."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A Block QUIC switch on a set's UDP tab, and a UDP action that leaves matched packets alone"})," - pushing a browser off QUIC and back onto TCP took two settings in two places with nothing saying they belonged together, and every action a set could pick altered the traffic, so a set matching QUIC only to learn a site's addresses had to fake and fragment it anyway. ",(0,i.jsx)(n.code,{children:"Sets > UDP"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Two of the three QUIC matching values did the same thing"})," - one read as turning QUIC matching off and the other as merely parsing, while both matched the set by the server name in the ClientHello. They are one value, By SNI, and stored sets are converted on the first start."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"An IPv6 packet carrying extension headers could crash b4"})," - a header claiming more bytes than the packet held sent the read past its end, stopping the service for every device on the router."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Force IPv4 for matched domains, which drops the IPv6 records from answers for names a set matched"})," - b4 processes IPv4 only until IPv6 support is turned on, so a dual-stack site a set targets was reached over IPv6 and bypassed the set entirely. ",(0,i.jsx)(n.code,{children:"Settings > Core > DNS"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A DNS answer that b4 rewrote never cleared the failure count for that name"})," - the answer was judged before the rewrite rather than after, so a site that had recovered was moved to its backup set on the next single failure."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"An egress IP for a set, which replaces the source address of its traffic"})," - a set could only be steered by naming an interface or an upstream SOCKS5 proxy, which a router that picks its path by source address cannot use. ",(0,i.jsx)(n.code,{children:"Sets > Routing"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A device added by hand was ignored everywhere devices are matched"})," - such a device has no MAC address on the network, so a set bound to it stopped taking its route, block sets and the all-devices-except-selected option were disarmed, the filter under Settings, Device Filtering sat inert while routing kept applying to every device, and an MSS clamp on it clamped nothing while pulling every other forwarded connection down to the smallest configured size. Such devices are matched by the IP address entered for them."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A routing set limited to source devices diverted traffic it was never meant to touch"})," - the rules on the router's own outgoing traffic carried no device filter, which paired with Match any IP address pulled its whole outbound path into the tunnel, and a set matched only by TCP or UDP port checked the port before any source at all while claiming its traffic was being diverted, which switched off every DPI countermeasure."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A routing set with no domain or IP target was built in full and quietly matched nothing"})," - every routing rule matches a destination address, so a set with none could never match a packet, and nothing above trace level said so."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Three unrelated services shared the API settings tab through a two-column grid and three different ways of showing a credential"})," - the IPinfo token was a plain field, the AI key a panel of chips and buttons, the MCP token a full-width field printing the secret in clear, and each service's enable switch sat among the fields it governed. The tab is called Integrations and gives every service one card whose header carries its on/off switch, one masked credential field with reveal, copy and generate, and for MCP a client configuration block that copies the endpoint and token as a ready-made JSON snippet."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsxs)(n.strong,{children:["b4 took the router down after a few minutes on a busy line, leaving ",(0,i.jsx)(n.code,{children:"runtime: program exceeds 10000-thread limit"})," in the log"]})," - every matched packet started its own injection task with no ceiling, and those tasks wrote to a raw socket that had no send timeout, so an outgoing interface that stopped draining held one kernel thread per task until the Go runtime refused to create more."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"The operating system thread count on the dashboard Runtime card"})," - the card reported goroutines but nothing about OS threads, which is the number the Go runtime stops the service over, so a runaway stayed invisible until the crash. b4 writes a goroutine dump to the log directory once the count passes half the limit."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1780---2026-08-17",children:"[1.78.0] - 2026-08-17"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"An MCP server at /api/mcp"})," - b4 spoke no protocol an external AI app could connect to. Read-only tools report status, configuration, strategy sets, domain coverage, recent connections, the log tail, metrics and diagnostics, and a resource per documented setting describes what that setting does. The model runs in the client app; b4 reaches no AI provider and needs no API key. Off by default, and authenticated by its own token, because a web login token is discarded on every restart. A second switch, also off by default, permits writes to any setting inside a set, to the MTProto and SOCKS5 subsystems and to the log level; credentials, the web server, the capture engine, the firewall backend and the packet marks are refused, and the last change can be reverted. ",(0,i.jsx)(n.a,{href:"https://docs.b4core.app/docs/settings/mcp",children:"MCP server"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Traffic that merely used port 53 was read as a name lookup"})," - a VPN or proxy tunnel hidden on that port had its encrypted contents taken for a domain name and matched against sets."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The server name read out of a QUIC connection was accepted whatever it contained"})," - any bytes of any length passed for a hostname, unlike the same name read from an ordinary HTTPS connection, which was checked."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Telegram reported the MTProto proxy as incorrectly configured and switched it off"})," - a session was accepted before b4 had reached a data center, and one blocked route was given longer than the client was willing to wait."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Data center 203 had no connection ready in advance"})," - spare connections were kept for two data centers only, so every session on the others started by opening one from scratch."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A route that answered nothing was set aside only when it replied with a rate limit"})," - a route that went silent cost every session after it the same full wait."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A name with several addresses was abandoned after the first one went unanswered"})," - the attempt stalled on that address while another address of the same name answered in under a second."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The API reference on the documentation site rejected the b4 username and password"})," - the credentials were sent to the service unchanged instead of being exchanged for a login token, so the Authorize window accepted a pasted token alone."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Turning the SOCKS5 proxy on or off, or moving it to another port, changed nothing until b4 was restarted"})," - the setting was saved and the save was reported as successful, but the listener was only ever started once at boot, so a proxy switched off in the interface kept accepting connections and one switched on never opened its port."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1770---2026-08-15",children:"[1.77.0] - 2026-08-15"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A Customize control on the dashboard: panels are reordered by dragging, widened or narrowed by dragging the right edge across a twelve column grid, and hidden with the eye, remembered per browser"})," - the order and the widths were fixed in the code, so a panel worth keeping an eye on, such as the MTProto proxy and the devices using it, sat below a domain list hundreds of rows long, there was no way to move it up, give it more room or drop a panel that never gets read, and a short panel beside a tall one left everything after it waiting below the taller one."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"The dashboard layout is kept in b4's own configuration rather than in the browser"})," - the order, the hidden panels and the widths were held in browser storage alone, so a layout arranged on one machine was absent from every other one, and a new browser, a private window or a cleared site cache put the dashboard back to its defaults. The browser copy is still written and is read when b4 cannot be reached, so the page keeps its arrangement while the service is restarting."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"The runtime, live signal and blackhole panels lay themselves out by their own width rather than the width of the window"})," - each read the browser's breakpoints, so a panel given less than the full width of the page kept a layout sized for the whole screen and had its contents cut off at the panel edge."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The packets b4 makes itself left a router by whichever uplink the main routing table picked, not by the interface the set they belong to is routed through"})," - fakes, split segments and desync packets are sent by b4 rather than forwarded, so they carried only b4's own mark and the set's routing never applied to them. On a router balancing two WANs the connection opened over one and continued over the other, from a different public address, and the destination reset it; with a set routed through a tunnel those packets, and the server name inside them, went out over the plain uplink instead."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"A set's hand-picked firewall mark is refused, and one is assigned instead, when it carries every bit of the mark b4 puts on its own packets"})," - the two became indistinguishable, and the set's traffic was read as traffic b4 had injected."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"The enabled sets in the trace header, with domain lists counted rather than listed and proxy credentials left out"})," - a trace carried the system side of a report and nothing about what b4 was told to match, so a set that misbehaves had to be read without its targets, strategy or routing at hand."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A set routed through a proxy leaked two sockets and two pipes for every connection whose far side died without closing"})," - the count reached the router's open-file limit within about an hour, and b4 stopped working until it was restarted."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"A proxied connection carrying no data in either direction is dropped after an hour, and after five minutes once one side has finished sending"})," - a relay had no time limit at all once the proxy handshake was done."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"A set that cannot reach its proxy reports it in the log and in the diagnostics bundle"})," - the failed connection was recorded at trace level only, so a proxy refusing every attempt left nothing behind at the default log level. Traffic the set matched was accepted, held for the length of the connection timeout and then dropped, with nothing on record to say why."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A set whose MSS limit is scoped to IPv4 addresses no longer caps the IPv6 traffic of the devices it names"})," - the limit was written once per address family, and the family the set holds no addresses for fell back to matching the device alone, so every IPv6 HTTPS connection from that device was capped at the set's value regardless of where it was headed."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A site switched to a backup set still had its name looked up by the set that was failing"})," - the backup's pinned addresses, DoH server and forwarding target were never consulted for it, so a domain that only the backup could resolve stayed unresolvable however the escalation was set up."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"An unusable name lookup switches the site to the backup set"})," - NXDOMAIN, SERVFAIL and answers that carry no address count towards the switch, and the backup set answers the lookup that tripped it. Escalation only ever reacted to something that happens after a connection is already open, which a site with no address on record never reaches."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A destination that answers no connection attempt switches the site to the backup set"})," - b4 already spotted the address going unanswered and confirmed it by probing from the router itself, but that finding went nowhere except into cutting the device's attempt short. The most common way a site fails, its address dropped outright, had no path to the backup set."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The backup set only took over once a TLS handshake had been read out of the traffic"})," - a connection that never gets that far, and the opening packet of a connection to a site already switched over, were both handled by the set that was failing, so the backup set's SYN-stage settings never ran."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"How readily a site switches over was read from IP block detection, a separate feature that is off by default"})," - unanswered TLS handshakes, forged resets and unusable name lookups each got a control of their own on the Escalation tab. The single one on offer before governed forged resets only, so moving it changed nothing about the trigger that fires most."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Turning a backup set off destroyed the link to it"})," - the link was erased on the next save and the set could not be picked again until it was enabled, so a set switched off for a minute had to be wired back by hand."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A set routed through a proxy could not escalate any further"})," - the chain stopped at the first proxy hop, though it is built to run up to eight."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Only the single address a connection was headed for was handed to the backup set's routing"})," - a site behind several addresses had to fail once per address, and the entry aged out of the firewall while the switch was still in force, which put the traffic back on the failing path without a word."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A switch-over ignored which devices the backup set targets"})," - one device's failure moved every device on the network onto the backup set."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Editing a set drops only the switch-overs that the edit invalidates"})," - every switch-over in force was discarded on each save, including those for sites the edit did not touch."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The chain-length warning repeated for every packet"})," - once a site had been through all eight hops, each retransmission logged it again."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1763---2026-08-14",children:"[1.76.3] - 2026-08-14"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Name lookups from devices on the network went unanswered in TUN capture mode"})," - the answer b4 built was addressed to the router's own uplink instead of to the device that asked, because b4 read the sender out of the packet after the router had already replaced it on the way out. The device was left waiting on a lookup b4 had taken out of the network and answered somewhere nothing was listening."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A name lookup b4 could not trace back to a device was destroyed rather than left alone"})," - the sender was gone from the packet and nothing checked whether it had been recovered before the query was taken out of the network, so the device retried into the same hole for as long as the set stayed enabled."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A blocked name, a pinned address and a curated answer never reached the device they were meant for, in TUN capture mode"})," - all three are delivered as a packet b4 builds itself, and all three carried the router's address as the destination."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Every name lookup was recorded against the router rather than against the device that made it"})," - the source column on the Traffic page read as the uplink address for all of them, and a set narrowed to particular devices had nothing to match a lookup on."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"The number of name lookups b4 resolves at once is capped, and the rest go to the device's own resolver untouched"})," - each intercepted lookup started a five-second resolver of its own with nothing limiting how many could exist at the same time, so one slow or unreachable DoH server turned an ordinary browsing session into thousands of waiting lookups. A set that matches a large geosite category and also redirects DNS put every name on the network through that path."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"A lookup the router itself made is remembered for five minutes instead of two seconds"})," - it was recorded as a failed lookup and re-read from the kernel's connection table, in full, every two seconds for as long as the flow lived."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The addresses b4 read out of a packet pointed into the memory the next packet is read into, in TUN capture mode"})," - a set routed through a proxy or an interface writes those addresses to the firewall from a background thread, and by the time it ran the packet they came from could already have been replaced by an unrelated one."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1762---2026-08-12",children:"[1.76.2] - 2026-08-12"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Photos, videos and stickers stopped loading in 1.76.0 and 1.76.1"})," - the data center Telegram serves media from was addressed under another one's name, so every media session was carried somewhere that could not serve it."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Two of Telegram's addresses were carried to the wrong data center"})," - they answer as data center 4, but sit inside a block b4 read as data center 2."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Most of Telegram's addresses matched no data center, which left those connections almost no routes to try"})," - b4 knew five address blocks, and nearly half the connections in a capture from a censored network fell outside all of them."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"The address block b4 reads as data center 4 was narrowed to a quarter of its size"})," - the 768 addresses dropped from it replied to nothing."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"A Cloudflare Worker of your own is the last WebSocket route tried rather than the first"})," - Cloudflare takes a worker back part-way through a session, well after the connection is made, so it passed every check while video failed on it."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A route that took a request and returned nothing was handed the next connection anyway"})," - only a route that went quiet mid-session, with a request still outstanding, was ranked down."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"The Cloudflare Worker script in the documentation holds its relay open longer"})," - Cloudflare could cut off the part carrying data back from Telegram the moment the connection was accepted. Set the worker's compatibility date to ",(0,i.jsx)(n.code,{children:"2026-04-07"})," or later."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsxs)(n.strong,{children:[(0,i.jsx)(n.code,{children:"tgprobe"})," never tested the shared Cloudflare pool"]})," - it started from a blank configuration, so every on/off setting in it read as off."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"The Cloudflare Worker is asked for a data center address without a port"})," - data center 203 does not answer on 5222, one of the three ports Telegram serves the same endpoint on, so passing the port on killed media connections. The script in the ",(0,i.jsx)(n.a,{href:"https://docs.b4core.app/docs/mtproto",children:"Telegram documentation"})," is again the tg-ws-proxy script unchanged, and a Worker deployed from either page needs nothing done to it."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1761---2026-08-11",children:"[1.76.1] - 2026-08-11"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Telegram never got past loading, with an empty chat list, in the WebSocket bridge routing mode"})," - the bridge only ever went through a Cloudflare Worker, never Telegram's own route, the one the MTProto proxy had been taking all along."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A Cloudflare Worker that had stopped passing traffic kept being picked for new Telegram connections"})," - a session on a dead one was held for five minutes before it was given up."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The WebSocket bridge sent traffic to a different data center address than the device had asked for"})," - it kept one address per data center, while Telegram hands out many."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Connections b4 opens for itself went out with none of its own bypass applied"})," - a fail-open connection, made when a destination is already in trouble, was the one it left most exposed."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"b4 put its routing rules back only once they had disappeared completely"})," - part of a rule set going missing counted as healthy."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1760---2026-08-10",children:"[1.76.0] - 2026-08-10"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Every name lookup took five seconds once a set was routed through a proxy or an interface"})," - each answer for such a set was written into the firewall from the same thread that reads packets from the kernel, one ",(0,i.jsx)(n.code,{children:"ipset"})," process per address, so nothing was read for as long as those processes took. Packets arriving meanwhile were dropped by the kernel without a word, and a lookup for a name b4 has no interest in crosses that queue several times, so it was the one most likely to lose a packet and wait out the resolver's five-second retry."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"The addresses from a DNS answer are written to the firewall in the background, in one batch"})," - the same addresses were re-added on every answer for the name, at one process per address, which on a router is most of a second of work repeated for traffic that changed nothing."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A DNS request sent by the router itself was handed to b4 twice"})," - the rule that captures it sat in a chain reached from two places, so every such request paid the round trip to b4 and back a second time for no purpose."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"A burst of packets no longer overruns b4 while it is busy"})," - the kernel's hand-off buffer was left at the system default, a few hundred kilobytes shared by every packet in flight."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"System diagnostics reported jq and other tools as missing when they were installed on a USB drive"})," - the check never looked outside the router's own folders."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Pinned addresses for a domain"})," - when a CDN keeps answering with an address the firewall drops, no bypass strategy reaches the server at all, and the only remedies were a hosts file on every device or a ",(0,i.jsx)(n.code,{children:"dnsmasq"})," alias on the router, both redone by hand each time the CDN changed its addresses. Written in hosts file order, address first. A pin only applies to names the set already targets, which is easy to get wrong silently, so a pin naming something the set does not match says so and offers to add it. ",(0,i.jsx)(n.code,{children:"Sets > DNS"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A curated DNS answer could name the very address b4 was about to reset"})," - when every address in an answer was unreachable, the replacement was taken from the addresses remembered for that name without checking whether any of them had since been marked unreachable as well, so a device could be handed one and have the connection reset on its first packet."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Every address seen in a DNS answer is remembered, not only the ones that completed a handshake"})," - a name whose answers carry a single address, as the Meta CDN does, left nothing to fall back to once that address was dropped, because an address could only be remembered by connecting to it and the block is what stops the connection."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Telegram messages arrived while photos, videos and stickers did not, in the WebSocket bridge routing mode"})," - a client marks a connection as a media one by the sign of the data center number it sends in its opening handshake, and b4 took that number from the address the client had dialled instead, which carries no such mark. Every media session was therefore announced to Telegram as an ordinary one and answered by the wrong half of the data center. The address now settles which data center it is, the client's handshake whether it is the media one."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A TCP connection was listed under a set whose port filter excludes it"})," - a set narrowed to a single TCP port, or to one TLS version, still had its name written next to every TCP connection it matched by address, so a set built to take only UDP looked as though it kept taking TCP after being scoped away from it. The connection itself was already left alone; only the name in the table said otherwise."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The Traffic page said nothing about what happened to a DNS request"})," - which resolver answered it, whether the answer came from a pin, had unreachable addresses replaced, was turned into NXDOMAIN or dropped outright, all of that was written into the connection record and then discarded by the interface, which could only draw the five markers it had been taught. The documentation listed the outcomes as visible on that page while the page showed a bare UDP row."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A documentation page for DNS"})," - what b4 does with name lookups was written down only in pieces, beside each setting it belonged to. ",(0,i.jsx)(n.a,{href:"https://docs.b4core.app/docs/dns",children:"DNS"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Every firewall rule was torn down and rebuilt every few seconds on some systems"})," - the chain that catches DNS over TCP from the router itself was created with a priority named after a place it cannot sit, so the whole DNS table was rolled back; the rule monitor then found that table missing and restored all of b4's rules from scratch on its next pass, over and over. Each rebuild leaves a moment in which nothing is intercepted at all."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1750---2026-08-08",children:"[1.75.0] - 2026-08-08"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A domain was handled by a different strategy than the one Discovery found for it"})," - every result was published with all the domains of the run, so two applied results claimed the same domains and set order picked the winner."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A set applied from Discovery had no effect until b4 was restarted"})," - it was saved and listed in the interface while the traffic it named kept going out under the previous settings."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A warning when several enabled sets name the same domain"})," - only one of them handles that domain, chosen by its place in the list rather than by which strategy works."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A switch for matching every domain, and one for matching every address"})," - a set meant to catch everything had to be written by hand as ",(0,i.jsx)(n.code,{children:"regexp:.*"})," or ",(0,i.jsx)(n.code,{children:"0.0.0.0/0"}),", and neither form appears anywhere in the interface. The IPv6 half, ",(0,i.jsx)(n.code,{children:"::/0"}),", was easy to leave out, so a catch-all set passed IPv6 traffic through untouched without saying so. Typing ",(0,i.jsx)(n.code,{children:"*"}),", ",(0,i.jsx)(n.code,{children:"any"})," or ",(0,i.jsx)(n.code,{children:"all"})," into either field adds the same entries, and ",(0,i.jsx)(n.code,{children:"*.example.com"})," is stored as ",(0,i.jsx)(n.code,{children:"example.com"}),", which was taken as written and matched nothing."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Two sets listing the same IP range fought over it unpredictably"})," - only one of them reached the matcher, and it was whichever was stored last rather than the one placed higher in the list. A set restricted to source devices lost the range outright to a general set below it, and the winner could change between restarts. Ranges of equal length are settled by set order, and a device-scoped set is offered the traffic first."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsxs)(n.strong,{children:[(0,i.jsx)(n.code,{children:"0.0.0.0/0"})," among a set's IP targets was dropped on the way to the firewall"]})," - ",(0,i.jsx)(n.code,{children:"ipset"})," cannot store a network with a zero prefix size, so on routers using iptables the routing set lost that entry, while per-set MSS clamping and duplication lost every entry alongside it, since those are loaded in one batch that fails whole. The matching engine had accepted the same entry all along, so a set matched traffic while its firewall side stood empty. Both catch-alls are written to the firewall as their two halves."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A set routed through a TCP-only upstream proxy left no firewall rules at all on routers using iptables"})," - each sync of the set failed on the name of its QUIC-refusing chain, one character over the length iptables accepts."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"DNS sent over TCP skipped the set's DNS server"})," - only requests over UDP were intercepted, so a device that switched to TCP reached the router's usual resolver instead."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A DNS section in Settings"})," - the port used for DNS over TCP, how long to wait for an answer, an off switch, and a check against ports other services already use."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"DNS request outcomes in the trace"})," - a trace named the domain but never whether the request reached the set's DNS server or slipped past it."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"One refusal from Telegram's WebSocket edge switched off a working Cloudflare Worker"})," - the refusal was treated as a verdict on every WebSocket route to that data center, including the relay the user runs themselves."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A paused Telegram data center address was answered with an instant failure"})," - Telegram read that as grounds to reconnect immediately, filling the log with hundreds of identical lines a second."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A warm spare connection to your Cloudflare Worker"})," - one ready connection per Worker and data center, in place of the 65-90 ms handshake every new Telegram connection paid for."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Traffic to a Cloudflare Worker was cut into one small write per Telegram message"})," - that framing was only ever required by Telegram's own WebSocket edge."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Several Worker addresses are tried in a random order"})," - they were tried strictly as written, so the first absorbed all the traffic and all the rate limiting."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"System diagnostics reported jq, sha256sum and nohup as missing while they were installed"})," - the check looked in fewer folders than a terminal session does."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Flow offloading was reported as a problem even when it had been set up to leave b4 alone"})," - the check could not tell a fast path that skips b4 entirely from one held back until b4 has seen the start of a connection."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"IP block detection also covers a destination that never answers its SYN"})," - the feature is described for addresses the firewall drops at the IP level, but it counted TLS ClientHello retransmissions, and a ClientHello exists only once a handshake has completed. An address-level block kills the handshake itself, so the single case the description named was the one case the check could not see, and what it did catch was a stateful block after the handshake."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"The router tests a destination itself before treating it as blocked"})," - packets going unanswered for one device is equally consistent with a slow server, a brief routing glitch or an uplink outage, so acting on that evidence alone resets connections to a destination nothing was ever wrong with, and an uplink that drops for a minute looks indistinguishable from every destination being blocked at once."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A destination marked blocked stayed blocked for as long as anything kept trying it"})," - each lookup refreshed the entry's timestamp, so the five-minute expiry was never reached while a device kept retrying, and nothing re-tested the address in the meantime. A false positive, or a block that had since been lifted, held for the lifetime of the traffic."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"An IP Health section in Settings"})," - whether an address answers depends on the address and the path to it rather than on the set that happened to match it, so the verdict is one shared judgement rather than a per-set one, and how long it lasts had no control of its own."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A switch that strips unreachable addresses out of DNS answers"})," - a CDN answers with several addresses of which the firewall drops only some, so one device hangs on a blocked one while another on the same network loads the site. Pinning a working address by hand in the hosts file or as a ",(0,i.jsx)(n.code,{children:"dnsmasq"})," alias had to be done per address, per domain, and again whenever the CDN changed its addresses."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Import from another tool"})," - a ",(0,i.jsx)(n.a,{href:"https://github.com/dovecoteescapee/byedpiandroid",children:"byedpi"})," or ",(0,i.jsx)(n.a,{href:"https://github.com/bol-van/zapret",children:"zapret"})," configuration had to be rebuilt in b4 by hand, option by option. A pasted command line becomes one set per profile, and every option is reported as carried over, approximated, or without an equivalent in b4. Domains are asked for per set, since such a command line rarely carries the host list itself. ",(0,i.jsx)(n.code,{children:"Sets > Import"}),"."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1742---2026-08-02",children:"[1.74.2] - 2026-08-02"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"b4's own output filled a router's memory"})," - init scripts from older installers wrote b4's full log to a file kept in RAM on OpenWrt, and updates never replaced those scripts."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A restart could leave a second copy of b4 running alongside the first"})," - the departing process deleted the file that tracks the running service, after its replacement had already claimed it."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Log files kept growing while b4 ran"})," - the error log was checked against its size limit only at startup, the update log was never trimmed, and abandoned trace files were left behind."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'Turning "Route UDP through upstream" off left the UDP rules in the firewall and took the listener away'})," - the rule state was keyed on the upstream host, port and username alone, so the switch never rebuilt the firewall side, while the listener was rebuilt without its UDP half. Matched UDP kept being diverted to a port with nothing behind it, so a browser speaking HTTP/3 to a routed site sat through its timeout rather than falling back to TCP, and switching the option off made the site less reachable than leaving it on. Only a restart applied the change."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"A set routed through a TCP-only upstream proxy refuses QUIC instead of letting it past"}),' - with "Route UDP through upstream" off, matched UDP was not diverted at all, so any site offering HTTP/3 was reached over QUIC straight out of the router while its TCP traffic went through the proxy. The bypass was silent, and a browser kept preferring it for a month after a single visit, since sites advertise HTTP/3 with a long ',(0,i.jsx)(n.code,{children:"alt-svc"})," lifetime. Matched UDP on port 443 is refused with an ICMP port-unreachable, which browsers read as a signal to fall back to TCP. Turn the option on if the upstream implements UDP ASSOCIATE."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"An upstream proxy that cannot relay UDP reported it only at trace level"}),' - matched UDP was dropped without a word, so a set pointed at a TCP-only SOCKS5 with "Route UDP through upstream" left on looked like a broken site rather than a setting to change.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Traffic handed to an upstream SOCKS5 proxy went out direct once for every address behind a domain"})," - a set matches by domain suffix, so ",(0,i.jsx)(n.code,{children:"ipinfo.io"})," covers ",(0,i.jsx)(n.code,{children:"website-cdn.assets.ipinfo.io"}),", while the address pre-resolve only looked up the names written in the set. Every other hostname was learned one address at a time from connections that had already left unrouted, and a site spread across a CDN kept producing them. A hostname that matches a routing set by suffix is resolved in full, and every address it answers with enters the set."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The first connection after a redirected DNS answer could escape a routing set"})," - b4 sent the answer to the client and only then wrote the addresses into the set, and writing them runs the ",(0,i.jsx)(n.code,{children:"nft"})," or ",(0,i.jsx)(n.code,{children:"ipset"})," binary, which takes longer than the client takes to send the packet that follows its DNS answer."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A Remove button for each geo database"})," - a downloaded ",(0,i.jsx)(n.code,{children:"geosite.dat"})," or ",(0,i.jsx)(n.code,{children:"geoip.dat"})," could only be replaced, never taken off the device, and pointing the destination directory somewhere else left the previous copy at the old path, since editing that field alone changes nothing to save. On a router with little free storage that meant two copies of a 51 MB file with no way to reach either from the interface. Remove deletes the file and clears its path and source URL, so the scheduler does not fetch it back, and a download or upload into a different directory deletes the copy b4 wrote at the old location."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"An update ran out of space on devices with little storage"})," - it kept a second copy of the installed b4 alongside the new one, and a failed update left that copy behind."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"An IPv4/IPv6 switch in the DC Relay socat helper"})," - the generated commands could only reach Telegram over IPv4, so on a VPS whose working route to the data centers is the IPv6 one, the addresses had to be found elsewhere and pasted in by hand every time a host was lost. The switch emits ",(0,i.jsx)(n.code,{children:"TCP6:"})," upstreams, and a DC Relay address written as an IPv6 literal (",(0,i.jsx)(n.code,{children:"[2001:db8::1]:7007"}),") also flips the listening side to ",(0,i.jsx)(n.code,{children:"TCP6-LISTEN"}),"."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1741---2026-07-31",children:"[1.74.1] - 2026-07-31"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A domain added to the watchdog was listed as Healthy before anything had checked it"})," - the entry was created green and counted toward the healthy total, so a site the user already knows is blocked read as fine until the first check landed, and with the watchdog switched off it read that way forever. A domain waiting for its first check is marked Queued, and the force-check button is disabled while the watchdog is off, where it did nothing."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The watchdog reported a site as healed while it was still unreachable"})," - a healing run crowned a strategy after one successful fetch, and against a filter that blocks intermittently one of the twenty strategies it tries lands in a gap by chance. That result went straight into the set and the domain was marked healthy, with nothing checking whether ordinary traffic to the site worked afterwards. A healed strategy has to reproduce the fetch several times and survive a re-check through the live engine, or the configuration is put back."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A healing run could quietly replace a hand-tuned set"}),' - it overwrote the whole tcp, udp, fragmentation and faking section while logging only the fragmentation strategy name, so a switch between two combo variants read as "combo -> combo" and every other tuned value disappeared without a trace.']}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A fetch that returned nothing counted as a working strategy during discovery"})," - a response without a declared length was accepted as complete even when zero bytes of the page arrived."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The fake packet sent before a blocked greeting did not resemble the greeting it shadowed"})," - it carried whatever length the fake payload happened to be, in one common case more than twice the real greeting, leaving a filter that reassembles the connection an untouched copy of the real bytes to read. Per-set ",(0,i.jsx)(n.code,{children:'fake_len_mode: "match"'})," sizes it to the greeting."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'The fake greeting\'s TTL was ignored unless the faking strategy was set to "ttl"'})," - a set could carry a TTL value while its fake ClientHello still went out at the full system TTL and reached the far end. The SYN fakes read the same value whatever the strategy is, so the setting looked live while doing nothing on the path that mattered. Per-set ",(0,i.jsx)(n.code,{children:"apply_ttl"})," applies it to the greeting alongside any faking strategy."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"The TCP MD5 option on the fake greeting itself"})," - ",(0,i.jsx)(n.code,{children:"md5_on_fake"})," makes the far end discard the fake while a filter in between still reads it. The option only ever went on a separate fake connection opening, and only alongside a fragmentation strategy."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"The fake and the real packet no longer share an IP identification number"})," - one field gave away that both came from the same source."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Fake-only discovery strategies"})," - a low-TTL fake with no fragmentation at all, signed and unsigned, and the same shape across the per-family TTL sweep. Every strategy that carried a fake also split the greeting, and the family sweep is built on a base whose fragmentation strategy is combo, so against a filter that reassembles TCP well enough to see through any split, this shape was out of reach at every phase of discovery."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"The TLS split position can follow the domain name"})," - ",(0,i.jsx)(n.code,{children:"middle_sni"})," on a set using the ",(0,i.jsx)(n.code,{children:"tls"})," fragmentation strategy places the split inside the domain name instead of a fixed offset from the record header."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"The STUN fake payload ships with b4"})," - ",(0,i.jsx)(n.code,{children:"tls_stun.bin"}),' is built in and picked as "Preset: STUN" under ',(0,i.jsx)(n.code,{children:"Fake Payload Type"}),". It had to be uploaded as a capture file on every device, and a set shared with someone else arrived pointing at a ",(0,i.jsx)(n.code,{children:".bin"})," they did not have."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Import of a shared set said nothing when the set referenced a payload file the device does not have"})," - the set pointed at a ",(0,i.jsx)(n.code,{children:".bin"})," capture that only existed on the machine it came from, the fake packets silently fell back to a built-in payload, and the set behaved differently than for the person who shared it. The import screen names the missing files and links to the Payloads settings."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"b4 would not start on an OpenWRT router whose kernel has no packet-queue module"})," - startup ended with a raw firewall error that pointed nowhere near the missing kernel module. ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/275",children:"#275"})]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The installer reported the router as ready when it could not run b4 at all"})," - the check passed if any one of three queue modules was present, so a router carrying the wrong one installed cleanly and failed at first start."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The diagnostics report showed no queue number, worker count or queue mode"})," - it read them from config keys b4 has never written, so those lines were skipped on every router."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A failed start left part of b4's firewall rules on the router"})," - when applying the rules stopped partway, the table, chains and hooks created up to that point stayed behind after b4 exited."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A kernel without connection packet counters stopped b4 from starting at all"})," - the whole queue rule was rejected and startup failed, over an optimisation that was never required."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"System Info listed the wrong kernel modules on nftables-only routers"})," - it marked the iptables-era modules as missing and said nothing about the nftables ones b4 depends on there."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'Telegram on Android could sit at "Connecting" when a set routed it through the built-in Telegram bridge'})," - the bridge closed connections that stayed silent for five seconds, though Telegram opens them before it has anything to send. ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/277",children:"#277"})]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'The "already exists in other sets" warning missed overlaps that do change routing'})," - it compared entries character for character while traffic is matched by domain suffix, so ",(0,i.jsx)(n.code,{children:"example.com"})," in another set drew no warning for ",(0,i.jsx)(n.code,{children:"www.example.com"}),". ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/273",children:"#273"})]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The DC Relay socat helper generated commands that pointed at the wrong Telegram servers"})," - it built them from Telegram's published proxy list, whose addresses drop the connection right after the handshake."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The first request to a domain after an idle spell could hang for twenty seconds or time out, and YouTube on Android could sit for minutes before its interface appeared"})," - when a client sent a TLS ClientHello too large for one packet, the domain name could land in the second one; b4 read each packet on its own and never joined them, so the set did not match and the connection went out with no bypass strategy at all. Chrome reorders the fields inside that greeting on every connection, which is why the same domain failed on one attempt and loaded on the next. ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/279",children:"#279"})," ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/280",children:"#280"})]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The first connection to an address b4 had just resolved for a client went out without the set that owned the domain"})," - nothing linked a DNS answer, or a domain seen in a rejected QUIC attempt, to the connection that followed it, so until some connection produced a readable domain name the traffic was treated as unknown. That gap was the one place where a domain b4 had already seen went unrecognised, and on Android YouTube it could leave the app retrying for minutes. The link is remembered per client and destination and is discarded when two domains on one address belong to different sets. ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/279",children:"#279"})]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"One device could decide which set another device's traffic went through"})," - the domain learned for a shared CDN address was stored once for the whole network and overwritten by whichever device connected last, so on an address serving both YouTube previews and video, one phone's choice was applied to everyone else. Evidence a device produced itself, from its own DNS answers or QUIC attempts, is preferred over the shared record. ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/279",children:"#279"})]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A plain connection opening handshake was taken apart and re-sent for no reason"})," - any set with fragmentation or fake-SNI turned on pulled every packet without content out of the kernel and pushed it back through a raw socket, including the opening SYN, although none of those techniques act on a packet that carries no data."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A set built from a large geosite category took several times more memory than its domains"})," - reading a few categories out of a 51 MB file needed around 90 MB, and the matcher and a switched-off SOCKS5 server each kept a spare copy of every domain."]}),"\n"]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1730---2026-07-05",children:"[1.73.0] - 2026-07-05"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A phone that dropped off mobile data could leave a Telegram proxy connection stuck"})," - when a mobile client backgrounded Telegram or lost signal, its connection frequently died without a clean close, yet b4 held the half-dead session and its link out to Telegram open for many minutes, in some cases until the app was reopened, so returning to Telegram could mean waiting on a dead socket instead of a clean reconnect."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A stalled link out to Telegram over a WebSocket bridge could wedge a proxy session"})," - if that link stalled mid-transfer, tearing the session down could itself block for many minutes on the stuck write, holding the client's slot and both connections until a kernel timeout expired."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Tunable MTProto connection timeouts"})," - Settings \u2192 MTProto exposes a TCP user timeout and an idle timeout for the proxy, for unusual carrier networks or router kernels where the built-in defaults (about two minutes and five minutes) need adjusting or turning off."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"API to list active MTProto sessions and clients"})," - two HTTP endpoints for integrations: GET /api/mtproto/sessions returns one entry per live client connection (client IP and port, the Telegram destination, and when it connected and was last active), and GET /api/mtproto/active-clients groups those by secret (active connection count and the distinct client IPs using each one). The API exposed only a per-secret connection count, with no way to see the client addresses behind it."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1720---2026-07-02",children:"[1.72.0] - 2026-07-02"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A separate MTProto secret for each person"})," - the MTProto proxy can now hold several named secrets instead of one shared code. Each one can be named (for example, per person), switched on or off on its own, and shared as its own link or QR code, so access can be given or taken away one person at a time."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"See who is using the MTProto proxy"})," - the Traffic page now labels each proxy connection with the secret it used, and a new dashboard panel shows active connections, total sessions, and data used for each named secret."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"A set can now apply to every device except chosen ones"}),' - in a set\'s Source Devices there is a new switch, "All devices except selected". With it on, the set applies to traffic from all devices except the ticked ones, instead of only to the ticked ones - handy when one device (say, a work laptop) should be left alone.']}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"The set's device list shows each device's MSS value"})," - if a device has an MSS limit set under Settings \u2192 Device Filtering, the set's Source Devices table now shows it next to the device, so there is no need to switch pages to check."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Match a set by domain only, without learning its IPs"}),' - a per-set "Domain-only matching" switch under the set\'s Targets. With it on, b4 decides the set purely from the domain or SNI it can see and does not remember or route the IP addresses behind those domains. It is meant for shared CDN addresses (YouTube, Google): a video domain and an unrelated one such as preview images can share a single IP, so the address b4 learned for the first one pulled the second onto the wrong bypass strategy.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Memory use jumped after saving settings"})," - saving the configuration made b4's memory use climb, even for changes that had nothing to do with the blocked-site lists."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A routing set could stop adding new addresses on some routers"})," - on certain setups (for example b4 in a container on MikroTik) the router rejected freshly discovered addresses, so a set routed through a chosen interface, proxy, or the Telegram bridge slowly stopped working. Those addresses are now stored in a form the router accepts. ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/267",children:"#267"})]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'The log repeated "Tables rules missing, restoring..." every few seconds'})," - with NAT Masquerade turned on, b4 looked for one of its firewall rules in the wrong place, decided it was missing, and needlessly re-applied all its rules over and over. The rules were in fact fine; b4 now checks the right place and the endless restoring has stopped."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1711---2026-06-29",children:"[1.71.1] - 2026-06-29"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"System Info now shows the router's kernel capabilities"})," - a new section reports whether the router supports the underlying features that advanced modes rely on."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Telegram bridge could be slow to connect or drop transfers"})," - in some setups b4 got in the way of its own connection out to Telegram, slowing the first connect and occasionally cutting transfers short; b4 now keeps its own traffic clear."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A named device sometimes showed as its address instead of its name"}),' - on the dashboard and Traffic page a device you had named could appear as a plain hardware address while it was off the network (for example connected over VPN); its saved name now stays shown, and named devices remain listed in Device Filtering with an "offline" label.']}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1710---2026-06-28",children:"[1.71.0] - 2026-06-28"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"The Logs page is easier to read"})," - log lines are now colour-coded by importance, and each type (error, warning, info, and so on) can be shown or hidden with one click."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Record a log session and download it to share"}),' - the Logs page now has a "Start trace" button that records everything until "Stop & save", which downloads a single log file to attach when asking for help. The file already includes system details, and recording stops on its own after 15 minutes.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"NAT Masquerade can now target several interfaces at once"})," - when NAT Masquerade is turned on (Settings \u2192 Feature), you can pick any combination of network interfaces for it to use \u2014 for example two out of three \u2014 instead of only a single interface or all of them."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Runtime panel on the dashboard"})," - shows b4's memory use over time, with a live chart and a couple of health readings, so a slow climb is easy to keep an eye on."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Per-device filtering (whitelist/blacklist) works in TUN mode"})," - the Device Filtering list under Settings only took effect in NFQUEUE mode; in TUN mode every device's traffic was inspected regardless of the selection, so choosing which devices use the bypass changed nothing there."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Live Signal panel reworked around throughput"})," - the connections- and packets-per-second readings hovered near zero on a home router and meant little, so they gave way to live throughput, total data handled, targeted share, and active connections."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Memory could slowly creep up the longer b4 ran"})," - a few background jobs (logging, AI chat streaming, Discovery) didn't always tidy up after themselves, so usage grew over long uptime."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"After stopping b4 in TUN mode, new sites could stop loading while already-open ones kept working"})," - on an unclean stop (a forced kill, or a shutdown that ran past its time limit) the virtual TUN interface disappeared but its routing rule could be left behind, steering fresh connections and DNS lookups into a path that no longer existed until b4 was started by hand again."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1701---2026-06-23",children:"[1.70.1] - 2026-06-23"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"System diagnostics now show the active engine and the firewall rules b4 has set up"})," - the diagnostics page tells you whether b4 is running in NFQUEUE or TUN mode (so TUN setups no longer look like a failed firewall), and lists the firewall rules b4 currently maintains, making it easier to see what is in place and share when asking for help."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"b4's internal firewall marks moved to a less-contended range"})," - b4 tags its own packets with a firewall mark (fwmark); the values it used overlapped the range Tailscale and some other router apps use, which could misroute traffic when they ran alongside b4. The internal marks now use high bits that other tools rarely touch."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Bypass stopped working for mobile apps (for example the YouTube app on Android) on gateway/container setups with NAT Masquerade"})," - the fake and fragmented packets b4 sends to defeat DPI were no longer given the gateway's public address, so they never reached the server. This mainly hit QUIC traffic, which phone apps prefer, while ordinary websites over TCP kept working."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A set could fail to start when it mixed a country/service list with its own listed addresses"})," - if an address you added by hand was already covered by a chosen list (for example adding Instagram addresses alongside the Facebook list), b4 saw them as overlapping and refused to start."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"In TUN mode every device appeared under the router's public address, so traffic could not be told apart by device"})," - the TUN engine tags packets with the router's WAN address before b4 inspects them, which flattened the connection logs, per-device statistics, and per-device set rules onto that one address and hid the phone, laptop, or TV behind it."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1700---2026-06-21",children:"[1.70.0] - 2026-06-21"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"New engine for devices without NFQUEUE"})," - some minimal devices lack the kernel modules b4 normally needs, so it could not run on them. A new mode under Settings \u2192 Feature Flags routes traffic through a virtual interface (TUN) instead. It inspects only the first packets of each TLS (port 443) and DNS connection, like the normal engine, rather than carrying every packet, and leaves the device's default route in place; on kernels that cannot count per-connection packets it routes the whole default route through the TUN instead. Strategy discovery (auto-tuning) still needs NFQUEUE, so it cannot run on these devices."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"TUN uplink can follow the default route automatically"})," - leave the uplink on Auto (the default) and b4 takes the outbound interface, gateway and source address from the current default route, re-pointing them whenever it changes; pick a specific interface to pin a fixed path instead. Without it the uplink was fixed at startup, so a WAN failover or an L2TP/PPP/VPN tunnel reconnecting after b4 had started left processed traffic going out the old, dead path and bypassing b4 until a manual restart."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Sortable columns in the Traffic page's Aggregated view"})," - clicking a column header sorts the grouped list by that column, ascending or descending, like the Raw feed."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Web UI password is now stored securely"})," - it is kept only as a hash and is no longer shown on the settings page (leave the field blank to keep it, or type a new one to change it)."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Dashboard rebuilt on one consistent design"})," - it now leads with a live signal panel that puts connection-rate and the key counters together, with every block sharing one panel, row, and header style."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A set's encrypted DNS (DoH) redirect failed in some setups"})," - lookups could time out or come back empty, so pages would not load. This affected gateway and container setups (for example b4 in a container on MikroTik) using NAT masquerade."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Routing set stopped updating its addresses on its own"})," - in some setups (for example b4 in a container on MikroTik) the set's addresses only filled in on restart or when the set was toggled, then expired and were never refreshed. The set now learns addresses live from DNS replies again."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Telegram bridge did not recover after an internet outage"})," - if the connection dropped and came back later, a set using the Telegram bridge kept failing until b4 was restarted by hand."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1691---2026-06-14",children:"[1.69.1] - 2026-06-14"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Sets manager felt frozen after enabling, reordering, duplicating, or deleting a set"})," - the screen only changed once the action had been saved and the whole configuration re-fetched a second or two later, so a click looked like nothing had happened, a dragged set snapped back to its old place before jumping to the new one, and a duplicate appeared out of nowhere after a pause."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"MTProto proxy stopped connecting to Telegram on networks that block its data-center addresses"})," - 1.69.0 changed the proxy to reach each Telegram data center at its own address, which many restricted networks drop, so connections timed out and Telegram would not load through the proxy."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1690---2026-06-14",children:"[1.69.0] - 2026-06-14"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Diagnostics now flag flow offloading"})," - many routers have a speed feature called ",(0,i.jsx)(n.code,{children:"flow offloading"})," that sends traffic down a fast path which skips b4 entirely, so b4 looks installed and running but nothing is actually bypassed (a common puzzle on OpenWrt). The system diagnostics (Settings system info, and the installer's diagnostics screen) now show whether flow offloading is switched on, so this can be spotted at a glance and turned off."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Connection limit for the MTProto proxy"}),' - a new "Max Connections" field in Settings -> MTProto Proxy caps how many client connections the proxy serves at once, with the built-in default raised from ',(0,i.jsx)(n.code,{children:"512"})," to ",(0,i.jsx)(n.code,{children:"2048"})," so a proxy shared by many people has room before it turns connections away."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsxs)(n.strong,{children:["Routing could clash with another router app such as ",(0,i.jsx)(n.a,{href:"https://daniellavrushin.github.io/asuswrt-merlin-xrayui/en/install",children:"XrayUI"})]})," - a set sent through a proxy or the Telegram bridge could be handled by that other app instead, so it only worked while the app was installed."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Discovery refused web addresses that contain a comma"})," - some perfectly valid links (for example certain Google image addresses) have a comma in them, which Discovery mistook for a separator between addresses and split the link into broken pieces, so it could not be added."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Could not pick individual results once a Discovery search had finished"})," - while a search was running you could add any of the configurations it turned up, but as soon as it stopped only the single best per group could be applied and the buttons to add the others disappeared. The buttons to add each result stay available after the search ends."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Discovery log was lost on page refresh and missing after a search finished"})," - the log was only streamed live, so refreshing the page wiped everything shown so far and left only the next incoming line, and a finished (especially quick) search often showed no log at all. The log is now kept on the service, so the full run is replayed whenever the page is opened or refreshed and stays readable after the search ends."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1680---2026-06-10",children:"[1.68.0] - 2026-06-10"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Discovery found nothing on connections that tamper with DNS"})," - on networks where the provider tampers with DNS (so ordinary name lookups came back wrong or empty), Discovery could not look up its test sites and reported that nothing worked, even when a working setup existed. It looks those sites up over encrypted DNS instead, and saves the result set to use encrypted DNS (DNS-over-HTTPS) for its lookups by default, which holds up where plain DNS is tampered with."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Cancelling Discovery could crash b4"})," - pressing Cancel while a search was testing several sites at once could crash the whole service and force a restart."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Stopping b4 could leave its firewall rules on the router"})," - the cleanup at shutdown could be undone at the last moment, so the rules stayed in place after exit."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Routing through another interface broke after that interface changed its address"})," - when the chosen outgoing interface (for example a VPN or a modem) got a new address, the set's traffic kept using the old one until b4 was restarted."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Incomplete cleanup on stop"})," - stopping b4 (or ",(0,i.jsx)(n.code,{children:"--clear-iptables"}),") left traces behind: adjusted system network settings, stray routing entries when two sets shared one outgoing interface, and a firewall rule that piled up with proxy or bridge routing."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Saving settings could leave the running configuration out of step"})," - changing the SOCKS5 proxy or your sets (adding domains or IPs, creating, editing, reordering, deleting, or bulk-toggling) could save a result that did not match what was actually running."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1672---2026-06-10",children:"[1.67.2] - 2026-06-10"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Web UI update reported success but kept the old version on some setups"})," - where b4 isn't managed by a normal service (for example running directly in a container), the update replaced the program on disk but never restarted the running copy. It now stops the old copy and relaunches the new one, so the update actually takes effect."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'Leftover "zombie" update processes piling up'})," - each update attempt left a finished helper process behind; these are now cleaned up."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Update log"})," - every Web UI update now writes a step-by-step trace to ",(0,i.jsx)(n.code,{children:"update.log"})," in the log folder (default ",(0,i.jsx)(n.code,{children:"/var/log/b4"}),", reset each attempt), making failed updates much easier to diagnose."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Localized changelog"})," - the Web UI now shows the changelog in the selected language."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Logging setting is now a folder, not a single file"})," - Settings \u2192 Service now asks for a log ",(0,i.jsx)(n.em,{children:"directory"})," (default ",(0,i.jsx)(n.code,{children:"/var/log/b4"}),") instead of a path to ",(0,i.jsx)(n.code,{children:"errors.log"}),", so all of b4's log files (errors, updates, and any added later) live together and can be moved in one place. Existing configs are migrated automatically (your old folder is kept); leave the field empty to turn file logging off."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1671---2026-06-10",children:"[1.67.1] - 2026-06-10"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Connections page slowed down the more network owners you looked up"}),' - looking up the "AS..." label for many addresses made the live list sluggish over time. It now stays responsive no matter how many you add.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Slow page failures when a set's encrypted DNS server was briefly unreachable"})," - lookups used to hang until they timed out; they now fail right away instead of stalling, and never fall back to plain DNS."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Cancelling Discovery did not stop it"})," - pressing Cancel left the search running, so starting another ran two at once with mixed-up, unreliable results. Cancel now stops the search promptly, and a new search waits until the previous one has stopped."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1670---2026-06-08",children:"[1.67.0] - 2026-06-08"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Encrypted DNS (DoH) for a set"})," - a set's DNS tab can now send its name lookups over an encrypted DNS-over-HTTPS connection instead of to a plain DNS server's IP. Some services only work when looked up through a specific resolver (for example ",(0,i.jsx)(n.code,{children:"xbox-dns.ru"}),' for sites that say "not available in your country"), and some providers tamper with ordinary DNS - encrypted DNS gets around both. Switch the set between "Plain DNS" and "DNS-over-HTTPS", then pick a server from the built-in list or paste your own address. Only that set\'s lookups are affected, so the rest of your DNS stays as it is.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Target a set to IPv4 or IPv6 only"}),' - a set can now be limited to only IPv4 or only IPv6 traffic, alongside the existing domain, IP, and TLS-version targeting. Some networks handle IPv4 and IPv6 differently, so the settings that work for a site over one can differ over the other. You can now make one set for a site over IPv4 and a separate set for the same site over IPv6, each with its own options. Discovery can also search for a working setup specifically over IPv4 or IPv6 and saves the result limited to that version. The default is "Any" (both), and the choice only appears when both IPv4 and IPv6 are turned on in Settings.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Sending a set's traffic to a proxy didn't work while bypass options were on"})," - when a set was set to route its traffic through an upstream proxy (or the Telegram bridge), b4 still tried to apply its DPI-bypass tricks to that same traffic, which fought with the routing and the connection failed to open. b4 now hands routed traffic straight to the proxy, so routing works even with the bypass options left enabled - and routed connections still show up on the Traffic page."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"RST protection did nothing on sets that also used SYN-based bypass"}),' - if a set combined "RST protection" with certain TCP bypass techniques, the protection was silently inactive because those connections weren\'t being tracked, so the site still got reset. RST protection now works in that combination. It also blocks the matching fake reset aimed at the destination server, not only the one aimed at your device.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Dummy network interfaces were missing from the interface lists"})," - a dummy interface (for example ",(0,i.jsx)(n.code,{children:"dummy0"}),") did not appear in Settings, so it could not be picked for monitoring or NAT masquerade. Any dummy interface that is up now shows up in both lists."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Telegram desktop kept reconnecting every 30-60 seconds over the Telegram bridge"})," - a background keep-alive added in 1.66.0 to hold idle connections open actually sent a signal Telegram's servers rejected, so the connection dropped and rebuilt itself about twice a minute (you would see parts of the Telegram window flicker). That keep-alive has been removed, so connections stay healthy on their own again."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Monitoring a site could add it to a blocking set and break it"})," - if you monitored a site (for example ",(0,i.jsx)(n.code,{children:"youtube.com"}),") and also ran a set that blocks or routes traffic - such as an ad-blocking set built from a category like ",(0,i.jsx)(n.code,{children:"category-ads-all"})," - the watchdog could mistake that set for the one that owns your site, because the block list happened to contain a sub-domain of it (for example ",(0,i.jsx)(n.code,{children:"ads.youtube.com"}),"). When the site dipped and the watchdog tried to repair it, it added the site to that block/routing set, so the site got blocked instead of restored. The watchdog now only repairs sets that list the site by name, and never touches a set that has Routing turned on, so monitoring can no longer poison a blocking or routing set. Note: if an earlier version already added the monitored site to such a set, remove it there by hand once."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1660---2026-06-07",children:"[1.66.0] - 2026-06-07"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Blocking stats on the Dashboard"}),' - when a set uses Block (blackhole) mode and actually blocks something, the Dashboard now shows a "Blackhole" panel with the total number of blocked attempts, the most-blocked domains, and which devices ran into the most blocks. The panel stays hidden until there is something to show, so it never clutters the page when nothing is being blocked. Blocked connections are also tagged with a "block" label on the Traffic page so you can spot them in the live feed.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Control over the Telegram server-list backup"})," - to reach Telegram, b4 fetches Telegram's data center list from Telegram's official address, and falls back to a backup copy hosted by the b4 author only if that is blocked (",(0,i.jsx)(n.code,{children:"lavrush.in"}),'). Settings -> MTProto Proxy now has a "DC list fallback mirror" switch to turn it off or point it at your own copy.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Set Routing now respects your device choices"})," - if you used Settings -> Devices to make b4 work for only some devices (or to exclude some), that choice was ignored by a set's Routing tab. Whatever the set did with the traffic (send it to another network interface or an upstream proxy, run it through the Telegram bridge, or block it) happened for every device regardless. Routing now applies only to the devices you picked. You can also give an individual set its own device list in its settings, so a single set can route, bridge, or block traffic for just specific devices without affecting the rest."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"The router itself showed up as a wrongly-named device on the Traffic page"})," - b4 mistook the router's own address for a regular client and listed it as a separate device (sometimes guessed as a phone brand), filing the router's own connections under it. b4 now recognizes its own interfaces and labels that traffic simply as \"Router\"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Discord voice and video calls could drop when blocking UDP"}),' - on a set with UDP set to Drop or Reject, Discord calls could break because b4 did not recognize Discord\'s call traffic. b4 now lets it through (when "Filter STUN" is on, the default), so calls keep working.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"b4 would not start on some Asus Merlin routers"})," - on certain firmware (for example the MerlinWRT) b4 quit right after starting and showed a confusing message about a missing ",(0,i.jsx)(n.code,{children:"xt_connbytes"})," feature, even though the router actually supported it. The real cause was b4 using a newer firewall command option that the router's built-in tool did not understand. b4 now adapts to the router's own tools automatically, so it starts normally with nothing extra to install.- FIXED: ",(0,i.jsx)(n.strong,{children:"Connections page showed nothing when the device clock was out of sync"})," - the time filter (30s / 1m / 5m / 15m) and the per-connection activity graph compared the live data against the browser's own clock, so a computer whose clock was off by even ~30 seconds could see an empty list and empty activity bars. Filtering and the activity graph now use the timestamps in the connection data itself, so they work regardless of the device clock or its timezone."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A device could still show up twice on the Traffic page"})," - one device sometimes appeared as two rows, once under its name and once under its bare IP. b4 now recognizes these as the same device and shows it only once."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Discovery found no working strategy when site names were entered with quotes"})," - if you added domains to Discovery wrapped in quotation marks (for example ",(0,i.jsx)(n.code,{children:'"discord.com","youtube.com"'}),", as happens when pasting a copied list), b4 kept the quote marks as part of each name, so every lookup failed and Discovery reported nothing working for any site. Surrounding quotes are now removed automatically. ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/241",children:"#241"})]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1650---2026-06-06",children:"[1.65.0] - 2026-06-06"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Block (blackhole) routing mode"}),' - a new "Block" option in a set\'s Routing tab that blocks all matched traffic (ad/tracker domains, IPs, or a GeoSite category like ',(0,i.jsx)(n.code,{children:"category-ads-all"}),") across the whole network - every LAN device and the router itself - with no output interface needed. It blocks by name and not just by IP, so it keeps working even with encrypted DNS and won't break unrelated sites sharing the same servers. See ",(0,i.jsx)(n.a,{href:"https://docs.b4core.app/docs/sets/blocking",children:"Blocking"})," for setup and details."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Upstream SOCKS5 proxy now routes UDP too"})," - sets that send traffic to an upstream SOCKS5 proxy used to forward only regular (TCP) connections; UDP traffic - such as QUIC (used by YouTube and many Google and video services) and DNS - bypassed the proxy and went out directly. UDP from LAN devices now goes through the upstream proxy as well, so a set can route a device's full traffic through it. The proxy must accept UDP."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"A device could appear twice on the Traffic page, and hand-added names were missing"})," - in the Aggregated view's Devices sidebar, one device sometimes showed up as two: once under its name and once under its bare IP address, because some of its connections arrived without a recognizable hardware address. Those connections are now matched back to the right device by its IP, so each device appears once. Devices you add by hand (Settings -> Devices), including ones behind another router, now show their assigned name here too instead of a raw IP."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsxs)(n.strong,{children:["Command-line options like ",(0,i.jsx)(n.code,{children:"--skip-tables"})," got saved into the config and stuck"]})," - starting b4 with an extra command-line option used to write that option into the configuration file, so it kept applying on later starts even when you ran b4 without it. Command-line options now affect only the run you pass them to and are never saved into the config."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1640---2026-06-01",children:"[1.64.0] - 2026-06-01"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"DNS redirect didn't work"})," - when a set sent its DNS lookups to a chosen server (the set's DNS tab), names failed to resolve and the internet seemed to hang. Redirected DNS now works as expected."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"MTProto Telegram routing is more reliable, especially for media"}),' - the WebSocket routing (bridge mode) now works on phones as well as desktop, loads media more reliably, and always prefers WebSocket regardless of the proxy server\'s transport setting. Data centers without a WebSocket edge no longer stall when the shared fallback domains get rate-limited. New "Cloudflare Worker" support adds a free, per-user relay (no domain to buy) for accounts where media, reactions, or stickers still fail to load - see Settings -> MTProto Proxy.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Copy buttons did nothing when the Web UI was opened over plain HTTP"})," - copying commands, set exports, or system info silently failed on installs reached by LAN IP (for example ",(0,i.jsx)(n.code,{children:"http://192.168.1.1"}),"), because browsers only allow the modern clipboard API over HTTPS or localhost. The fallback copy path now works in those cases too."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Memory and CPU slowly climbed when the watchdog repeatedly repaired a site"})," - each watchdog repair left behind a background task that never stopped, piling up over days until b4 was killed. ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/227",children:"#227"})]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'Traffic page lumped every connection under "Unknown" in the Aggregated view'}),' - on networks where b4 cannot map a client IP to a MAC address (for example devices behind another router), the Devices sidebar collapsed all traffic into a single "Unknown" device, even though the Raw feed clearly showed different source IPs. Connections without a known MAC are now grouped by their source IP, so each device appears separately.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Filter box did nothing in the Traffic page's Aggregated view"})," - multi-term filters like ",(0,i.jsx)(n.code,{children:"!github + !google"})," were treated as one literal text match, so the view usually showed nothing. The Aggregated view now uses the same filter syntax as the Raw feed: combine terms with ",(0,i.jsx)(n.code,{children:"+"}),", exclude with ",(0,i.jsx)(n.code,{children:"!"}),", and target a column with ",(0,i.jsx)(n.code,{children:"field:value"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"More checks in the DPI Detector"}),' - the DPI Detector page can now also measure how quickly your DNS servers respond and test whether Telegram works (download speed, plus whether all of Telegram\'s servers are reachable). The existing checks were made more accurate at telling apart the different ways a provider can block or tamper with a connection, the list of sites and servers it tests was refreshed, and a new "Legend" explains what each result means. The DPI Detector is based on the ',(0,i.jsx)(n.a,{href:"https://github.com/Runnin4ik/dpi-detector",children:"dpi-detector"})," project."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1630---2026-05-22",children:"[1.63.0] - 2026-05-22"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsxs)(n.strong,{children:["Geo databases sometimes saved to a broken ",(0,i.jsx)(n.code,{children:"b4"})," folder after install"]})," - in rare cases the installer would record ",(0,i.jsx)(n.code,{children:"b4/geosite.dat"})," instead of a full path like ",(0,i.jsx)(n.code,{children:"/etc/b4/geosite.dat"}),", and the Web UI then refused to download new files. The installer now refuses non-absolute paths, b4 fixes any broken path it finds on startup, and the UI falls back to a safe default if the stored path looks wrong."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Quick toggle for all sets"})," - new switch in the Sets page top bar that turns every set on or off in one click. Useful for temporarily disabling all bypass, or for isolating one set while debugging."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:'"Test direct TCP" button for the MTProto proxy'}),' - new button in Settings \u2192 MTProto Proxy next to "Test connection". Probes Telegram directly, bypassing the DC Relay. Use it to tell whether a problem is on your relay/VPS or somewhere between b4 and Telegram.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Auto-update for geo databases"}),' - new "Auto-update" section in Settings \u2192 Geo Databases. Optional "Refresh on startup" re-downloads both files every time b4 starts (handy when files are kept in ',(0,i.jsx)(n.code,{children:"/tmp"}),' and get wiped on reboot). A separate "Schedule" picker (Off / Daily / Weekly / Monthly) runs a background refresh on its own. Missing files are also re-downloaded automatically at startup whenever a source URL is set, even with both options off.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Per-set MSS Clamping"}),' - new "MSS Clamping" section in the TCP tab of each set, alongside the existing global and per-device options. Lets you set a custom MSS for the connections in one set - for example, a small MSS for a Smart TV streaming YouTube. The set must target by IP, GeoIP, or source device; SNI domain or GeoSite targets alone are not enough.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Direct TCP path used internal Telegram backend addresses instead of public ones"})," - b4 was reading addresses from Telegram's ",(0,i.jsx)(n.code,{children:"getProxyConfig"})," (a topology file for MTProxy operators) and dialling them directly. Those backends silently dropped the connection after the handshake because clients are not allowed there. b4 now uses the well-known public data center IPs for direct dialling, like real Telegram clients do."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"MTProto proxy ignored the DC Relay in Auto mode"}),' - when the upstream transport was set to "Auto" and a DC Relay was configured, b4 still went straight over WebSocket and never used the relay. The relay is now tried first in Auto mode whenever one is configured; WebSocket stays as the fallback. The mode description updates to reflect this.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"MTProto media data center (DC 203) did not work through the DC Relay"})," - DC 203 traffic was sent to a port that does not exist on any relay setup, so loading media through the relay failed. DC 203 now reuses the DC 2 relay port (matching the existing WebSocket behaviour), and has a built-in default IP for direct TCP as well."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Geosite/geoip updates didn't take effect until the next set edit"})," - after downloading or uploading a new geosite or geoip file from the Web UI, b4 kept matching against the old domain and IP lists. The new file is now applied to all sets immediately."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Some sites would not load through the built-in SOCKS5 server"})," - sites like YouTube failed when accessed via the SOCKS5 proxy while working fine in direct mode."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Per-device MSS could be overridden by Global MSS on iptables routers"})," - when Global MSS and per-device MSS were both enabled, the global value silently won on iptables-based routers (Merlin, OpenWrt without nftables, Keenetic). The more specific value now wins, matching how it has always worked on nftables."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Watchdog keeps checking while it repairs a broken site"})," - previously, when b4 detected one site was blocked and started repairing it, the watchdog stopped checking every other site until the repair was done. If a second site broke at the same time, it had to wait its turn. The watchdog now keeps monitoring all sites during a repair, and if several sites break close together they are repaired in one go."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"MTProto connection test now detects upstream drops"}),' - the existing "Test connection" button used to only check that an IP was reachable. It now also completes the MTProto handshake and watches whether Telegram (or your relay) closes the connection right after - the failure mode users actually hit. Results show which stage broke (connect, handshake, or dropped after handshake).']}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1621---2026-05-11",children:"[1.62.1] - 2026-05-11"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Memory limit setting"}),' - new "Memory Limit" field in Settings \u2192 Logging. Caps how much memory b4 may use. Leave empty for auto (half of system RAM, the previous default). Useful on routers with little RAM where other services compete for memory. Accepts values like ',(0,i.jsx)(n.code,{children:"128MiB"}),", ",(0,i.jsx)(n.code,{children:"256m"}),", ",(0,i.jsx)(n.code,{children:"1g"}),", or ",(0,i.jsx)(n.code,{children:"off"})," to disable."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"MTProto proxy works in censored networks"}),' - the built-in Telegram proxy can now reach Telegram over WebSocket, in addition to direct TCP. New "Upstream Transport" section in Settings \u2192 MTProto Proxy with three modes: Auto (WebSocket \u2192 TCP, the new default), WebSocket only, and Direct TCP. Existing installs are switched to Auto on upgrade, so networks without filtering see no change.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Cloudflare fallback for the MTProto proxy"}),' - optional "Cloudflare custom domain" field. If Telegram\'s own WebSocket endpoint is ever blocked too, pointing a Cloudflare zone at the Telegram data center IPs lets the proxy tunnel through Cloudflare instead.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"MTProto proxy was incompatible with some Telegram clients"})," - clients using transport variants other than padded-intermediate were silently dropped during the handshake. All three Telegram transport variants are now accepted, and the client's choice is forwarded to the data center."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Upstream SOCKS5 proxy routing did not work on some OpenWrt setups"})," \u2014 traffic skipped the proxy and went straight to the internet on installs missing two required kernel modules. The modules are now installed and loaded automatically, and a clear error is shown if they are still missing. ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/221",children:"#221"})]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Cleaner Import/Export for sets"})," - the exported JSON of a set now hides settings for features that are turned off, so you only see what actually matters. Easier to read, share and compare."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1614---2026-05-10",children:"[1.61.4] - 2026-05-10"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'False "another b4 instance is already running" error'})," - b4 could refuse to start after a crash, after restarting from the Web UI, or when running inside containers (for example on MikroTik), even when no other b4 was actually running. The single-instance check is now reliable in those cases."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1613---2026-05-09",children:"[1.61.3] - 2026-05-09"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Custom payload for UDP fake packets"}),' - new "Fake Packet Payload" picker in the UDP fake settings of each set. Choose a captured ',(0,i.jsx)(n.code,{children:".bin"})," (uploaded in Settings \u2192 Payloads, or auto-captured from live QUIC traffic) to use as the body of fake UDP packets. Empty = zero fill (previous behavior). The Settings \u2192 Payloads upload form now has an explicit TLS/QUIC protocol selector."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Auto-generated QUIC Initial payload"}),' - new "(auto: QUIC Initial)" option in the UDP Fake Packet Payload picker. b4 generates a fresh randomized QUIC Initial packet for every fake, with random connection IDs each time, so no upload is needed and the bytes can\'t be fingerprinted by repetition. Recommended packet size for this mode is 1200 bytes. Works with any Faking Strategy (None / TTL / Checksum).']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Bundled QUIC presets"}),' - two ready-to-try options ("QUIC preset 1" and "QUIC preset 2") in the UDP Fake Packet Payload picker. Pick one if the auto option does not work for your provider; if neither helps, upload your own ',(0,i.jsx)(n.code,{children:".bin"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"MTProto relay setup helper"}),' \u2014 new "?" button next to the DC Relay field in Settings \u2192 MTProto Proxy. Opens a popup with the current Telegram data center list and ready-to-copy commands for the VPS, so nothing has to be calculated or guessed by hand.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Single-instance enforcement"})," \u2014 b4 now refuses to start if another b4 process is already running on the same host, exiting with a clear message and the existing PID."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Sequence overlap length for fragmentation"}),' - new "Overlap Length" field for Combo and Disorder splitting. b4 prepends the configured number of pattern bytes to one of the real fragments with its TCP sequence number shifted back by the same amount, in addition to (or instead of) the existing fake-packet overlap.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"AI field explanations (experimental)"})," - small AI buttons next to some fields open a popup that explains what the field does. Work in progress: only a few fields are covered and answers can be wrong or incomplete. Not recommended to rely on yet."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Web UI error messages"})," - when saving a configuration or a set fails, the Web UI now shows a clear reason (for example, a port is already in use)."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Upstream SOCKS5 routing failed on BusyBox routers"})," \u2014 sets routed through an upstream SOCKS5 proxy were missed by the 1.49.0 fix and still hit the ",(0,i.jsx)(n.code,{children:"BusyBox"})," table-ID limit. Now kept within the safe range too."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"QUIC blocking sometimes does not work for YouTube on phones"}),' - newer Chrome versions on Android use a QUIC variant b4 didn\'t recognize, so "block all QUIC" let those packets through and YouTube kept working in the browser. b4 now recognizes any QUIC packet, current or future.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Routing fails on MikroTik containers when interface names contain dashes"})," - interface names with dashes were rejected by ",(0,i.jsx)(n.code,{children:"nft"}),". b4 now quotes interface names in routing rules, so any name works."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Interface routing did not work on MikroTik container bridges"})," - when several container interfaces shared one bridge with a single upstream gateway, the per-set default route was added without a gateway and traffic went nowhere. b4 now reuses the system gateway when it is reachable through the chosen interface."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Backup file could not be selected in Safari on macOS"})," - the file picker greyed out the downloaded ",(0,i.jsx)(n.code,{children:".tar.gz"})," backup file. Selecting the backup now works in Safari."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Voice calls, screen share and gaming could break in UDP fake mode"})," - b4 was splitting all matched UDP packets, which could disrupt voice, screen share and gaming traffic. Only QUIC packets are split now; the rest pass through untouched."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1601---2026-05-02",children:"[1.60.1] - 2026-05-02"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Per-set strategy escalation"}),' - each set now has an "Escalation" tab with an "Escalate to" dropdown. Pick another set as the failover target: if the current set keeps failing for a destination, b4 switches that destination to the chosen set on the next connection. Tracking is per-hostname, so a problem with one site does not affect others that happen to share the same server IP. How quickly to escalate and how long to keep the switch are configurable per set (defaults: an hour, then retry). Lets you chain several strategies instead of giving up after the first one fails.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Share MTProto proxy connection"}),' - new "Share connection link" button in Settings > MTProto Proxy. Opens a dialog with a ',(0,i.jsx)(n.code,{children:"tg://"})," connection link, a QR code (scan with your phone camera to add the proxy to Telegram), and Copy / Open in Telegram / Share buttons."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsxs)(n.strong,{children:["Service crashed at startup on routers without ",(0,i.jsx)(n.code,{children:"ipset"})]}),' - "Enable Packet Duplication" could prevent b4 from starting on routers where ',(0,i.jsx)(n.code,{children:"ipset"})," is not installed (some Keenetic / Merlin setups). b4 now logs a warning and keeps running. For full-connection duplication, install ipset (Keenetic / Entware: ",(0,i.jsx)(n.code,{children:"opkg install ipset"}),")."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'"Update" button in Web UI didn\'t actually update b4'}),' - on some setups clicking "Update" did nothing and the version stayed the same after restart.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"IPv6 addresses on the Traffic page were cut off"})," - when adding an IPv6 address from the Traffic page, the address was shown as just ",(0,i.jsx)(n.code,{children:"2a01"})," and the suggested CIDRs included the port number. Adding to a set, IPInfo lookup, and ASN enrichment now work for IPv6 too."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1501---2026-04-29",children:"[1.50.1] - 2026-04-29"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Upstream SOCKS5 routing didn't actually proxy"})," \u2014 when a set was configured to route through a SOCKS5 server (local or remote), traffic was silently going direct instead of through the proxy. Connections from the same machine running B4 now reach the upstream proxy correctly."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Duplicate masquerade rule when routing is enabled with IPv6"})," \u2014 on ",(0,i.jsx)(n.code,{children:"nftables"})," setups with both IPv4 and IPv6 enabled, the per-set NAT chain ended up with two byte-identical ",(0,i.jsx)(n.code,{children:"masquerade"})," rules. The rule is now scoped per address family (",(0,i.jsx)(n.code,{children:"meta nfproto ipv4"})," / ",(0,i.jsx)(n.code,{children:"ipv6"}),"), matching the split already used for the mark rules."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Error log keeps history across restarts"})," - the error log was wiped on every B4 start, so any trace of a crash was lost as soon as B4 auto-restarted."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:'"Connections" page renamed to "Traffic"'})," - clearer name for the page that shows live network activity. Old ",(0,i.jsx)(n.code,{children:"/connections"})," links still work and redirect to the new page."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Traffic page works at any log level"})," \u2014 previously, raising the log level above ",(0,i.jsx)(n.code,{children:"Info"})," made the Traffic page stop updating. The UI now gets traffic events through a separate channel, so you can keep the log level on ",(0,i.jsx)(n.code,{children:"Warn"})," or ",(0,i.jsx)(n.code,{children:"Error"})," to quiet down system logs without losing the live traffic view. Opening the Traffic page also instantly shows the last few hundred recent events instead of waiting for new traffic."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1500---2026-04-27",children:"[1.50.0] - 2026-04-27"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Refreshed UI"})," \u2014 the whole web UI has been redesigned: cleaner typography, tighter spacing, calmer colour palette, larger and easier-to-read numbers, and better keyboard accessibility throughout."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Sequence overlap pattern in Combo fragmentation"})," \u2014 the Combo fragmentation strategy can again be tuned from the UI: choose a preset (TLS handshake, HTTP GET, zeros) or build a custom byte pattern for the overlap step."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Upstream SOCKS5 routing"}),' - per-set routing can now forward matched traffic to a SOCKS5 proxy (local or remote) instead of out a network interface. Use this to chain b4 with Xray, sing-box, or any SOCKS5-speaking proxy. Pick "Upstream SOCKS5 proxy" mode in the routing tab and set the host and port.']}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1491---2026-04-20",children:"[1.49.1] - 2026-04-20"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Routing stopped working after restarting tun2socks / sing-box"})," - if the proxy's network interface was recreated, or wasn't ready yet when B4 started, traffic routing silently broke until B4 was restarted too. B4 now detects these changes automatically and restores routing within a second."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Connections page - Aggregated view layout"}),' - the set name was shown twice when a set matched both by domain and by IP, and the SOCKS5 "proxy" label could overlap the domain column.']}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1490---2026-04-18",children:"[1.49.0] - 2026-04-18"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Manual devices"})," \u2014 you can now add IP addresses of devices behind another router that are not visible in the ARP table. Added devices appear in device lists and can be used for per-device filtering and MSS clamping. Find it in Settings \u2192 Device Filtering. (",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/185",children:"#185"}),")"]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Fake payload from domain"})," \u2014 new option in ",(0,i.jsx)(n.code,{children:"TCP"})," fake settings to generate a fake TLS handshake from any domain you type (e.g. ",(0,i.jsx)(n.code,{children:"example.com"}),")."]}),"\n",(0,i.jsxs)(n.li,{children:["REMOVED: ",(0,i.jsx)(n.strong,{children:"Separate device alias file"})," \u2014 device names are now part of the main config. The old ",(0,i.jsx)(n.code,{children:"mac_aliases.json"})," file is no longer used (aliases are migrated into the config on upgrade)."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Traffic routing fails on Keenetic routers"})," \u2014 routing table IDs could be generated above ",(0,i.jsx)(n.code,{children:"255"}),", which is not supported on systems using ",(0,i.jsx)(n.code,{children:"BusyBox"}),". Table IDs now stay within the safe range."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Fake SYN used the wrong payload"})," \u2014 when ",(0,i.jsx)(n.code,{children:"Syn Fake"})," was enabled, the fake ",(0,i.jsx)(n.code,{children:"SYN"})," packet always used a built-in payload instead of the one selected in the set (custom, captured, or domain-generated)."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"GeoSite routing not working with local DNS proxies"})," \u2014 when the router forwards DNS through a local proxy like ",(0,i.jsx)(n.code,{children:"https-dns-proxy"}),", domains from ",(0,i.jsx)(n.code,{children:"GeoSite"})," categories were not added to routing sets. B4 now intercepts DNS queries earlier so routing works in these setups."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'Log level "Error" reverted to "Info" after restart'})," \u2014 when the log level was set to ",(0,i.jsx)(n.code,{children:"Error"})," in the UI, it was silently changed back to ",(0,i.jsx)(n.code,{children:"Info"})," on the next start."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"MTProto images and files not loading"}),' \u2014 Telegram uses extra "CDN" data centers (like DC 203) for media that B4 did not know about, so some pictures and files failed to load. B4 now fetches the current Telegram data center list on start (and on demand from Settings \u2192 MTProto Proxy \u2192 "Refresh Telegram DC list"). (',(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/190",children:"#190"}),")"]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Discovery groups domains more consistently"})," \u2014 when one strategy works for all tested domains, they are shown as one group and can be applied together in a single click, instead of being split into separate groups."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"RST protection catches more fake resets"})," \u2014 added extra checks that help tell real resets from injected ones, so fewer connections are killed by DPI."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Packet handling on routers with extra firewall marks"})," \u2014 when other services (VPN, QoS, policy routing) set their own marks on packets, B4 did not recognize its own mark. This could cause queued packets to loop and, when traffic routing was enabled, break masquerade on VPN interfaces."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Connections page \u2014 Aggregated view"}),' \u2014 new default view groups packets by device, protocol and domain (or IP when no domain is seen), with an activity chart per group and a side list of devices you can click to filter. The old table is still available under the "Raw feed" tab.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Floating save button in set editor"})," \u2014 a save button now stays in the bottom-right corner while editing a set, so you can save without scrolling back to the top."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Reset config to defaults is back"})," \u2014 the reset button in Settings \u2192 Core Controls has been reworked and returns. Your sets, web server settings, and geo file paths are preserved so the UI stays reachable."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1481---2026-04-05",children:"[1.48.1] - 2026-04-05"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"UDP Reject mode"}),' \u2014 new option for QUIC/UDP handling that sends an ICMP "Port Unreachable" response instead of silently dropping packets. Clients fall back to TCP almost instantly instead of waiting for timeouts.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Duplicate devices in device list"})," \u2014 when a device changed its IP address, both old and new IPs appeared as separate entries. Now only the current IP is shown."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1480---2026-04-05",children:"[1.48.0] - 2026-04-05"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Config backup before update"})," \u2014 B4 now automatically saves a copy of your config file before updating (e.g. ",(0,i.jsx)(n.code,{children:"b4.json.bak.v1.47.2"}),"), so you can restore it if needed."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Backup reminder in update dialog"})," \u2014 a warning in the update dialog reminds you to download a full backup from Settings \u2192 Backup before updating."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Watchdog"})," \u2014 background service that monitors configured domains and automatically finds a working bypass when DPI starts blocking them. Add domains or URLs to monitor, and watchdog periodically checks connectivity. After consecutive failures, it runs a quick discovery, applies the first working config, and resumes monitoring. Multiple failed domains are healed together in a single discovery run. Domains with the same working strategy are grouped into one set. Configure intervals, retries, and cooldowns in Settings > Discovery. Live status on the new Watchdog page."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"IP block detection"})," \u2014 B4 can now detect when a destination IP is blocked entirely (not just by domain name). When detected, B4 immediately resets the connection so your device retries faster on a different server instead of waiting for a timeout."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"RST injection protection"})," \u2014 B4 can now detect and drop fake TCP RST packets injected by DPI systems to kill your connections. Enable per set in TCP settings. Uses three independent checks: TTL fingerprint mismatch, RST arriving before any server response, and multiple RSTs on the same connection."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Fake strategy settings shown only when relevant"})," \u2014 Sequence Offset now only appears for ",(0,i.jsx)(n.code,{children:"Past Sequence"})," and ",(0,i.jsx)(n.code,{children:"Random Sequence"})," strategies, Timestamp Decrease only for Timestamp strategy. Reduces clutter in the UI."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Error when manual IP overlaps with GeoIP"})," \u2014 adding an IP address manually that already exists in a ",(0,i.jsx)(n.code,{children:"GeoIP"})," category within the same set caused a firewall error. Duplicate IPs are now filtered automatically."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Traffic routing stops working after ~1 hour"})," \u2014 ",(0,i.jsx)(n.code,{children:"GeoIP"})," addresses and static IPs were added to firewall sets with a ",(0,i.jsx)(n.code,{children:"TTL"})," and silently expired. They are now permanent. DNS-resolved IPs are periodically refreshed before they expire."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1472---2026-04-01",children:"[1.47.2] - 2026-04-01"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Discovery tests multiple domains in parallel"})," \u2014 when checking several URLs, discovery now tests all domains at the same time instead of one by one, making it noticeably faster."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1470---2026-03-31",children:"[1.47.0] - 2026-03-31"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Routing breaks when VPN/WireGuard restarts"})," \u2014 B4 now automatically detects when a network interface changes and refreshes routing rules. Previously, restarting WireGuard (or any VPN) while B4 was running would silently break routing until B4 was restarted."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Discovery no longer interrupts normal traffic"})," \u2014 discovery now runs on its own isolated flow, so your internet connection stays unaffected while discovery is testing strategies."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Stopping discovery is now reliable"})," \u2014 cancelling a running discovery now properly cleans up all firewall rules and stops immediately."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Discovery results grouped by strategy"})," \u2014 when multiple domains share the same bypass strategy, they are shown as one group instead of separate cards. One click applies the config to all domains at once."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Discovery UI redesigned"})," \u2014 cleaner layout with responsive grid for large screens, simplified logs (button + modal instead of inline panel), and expandable per-domain details during live testing."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Removed confusing speed numbers from discovery"})," \u2014 raw MB/s values that users mistook for browsing speed are replaced with ranked preset lists and improvement percentages."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'Discovery history always showed "just now"'}),' \u2014 timestamps were not saved correctly, so all history entries appeared as "just now" regardless of when they ran.']}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Swagger UI moved to documentation site"})," \u2014 the embedded Swagger UI has been removed from the binary to reduce its size (~27MB \u2192 ~13MB). API documentation is now available at ",(0,i.jsx)(n.a,{href:"https://docs.b4core.app/swagger",children:"docs.b4core.app/swagger"})," with the ability to connect to a live B4 instance for testing. The ",(0,i.jsx)(n.code,{children:"/swagger/"})," endpoint now redirects to the documentation site."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"System diagnostics in UI"}),' \u2014 new "System Info" button in Settings shows full system diagnostics: OS, kernel, memory, network interfaces, firewall status with NFQUEUE test, kernel modules, installed tools, geodata status, storage, and all B4 paths. Copy as JSON for easy sharing.']}),"\n",(0,i.jsxs)(n.li,{children:["REMOVED: ",(0,i.jsx)(n.strong,{children:"Config reset button"}),' \u2014 the "Reset to defaults" function has been removed from Settings.']}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Empty configuration sets allowed"}),' \u2014 B4 no longer forces a default empty set to exist. You can now delete all sets, and the Sets page shows a clean empty state with a "Create Set" button.']}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Config file is now compact"})," \u2014 only settings you changed are saved to the config file. Default values are no longer written, making the file much shorter and easier to read."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1466---2026-03-26",children:"[1.46.6] - 2026-03-26"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"B4 fails to start with large configs"})," \u2014 configs with many sets and hundreds of thousands of IPs caused an error on restart because all IPs were inlined into a single firewall command."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Routing skips IP ranges"})," \u2014 when IP ranges (e.g. ",(0,i.jsx)(n.code,{children:"10.0.0.0/24"}),") were added to a set's target IPs, routing silently ignored them and only routed individual IPs. Now all IP ranges are routed correctly."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Update from UI not working"})," \u2014 updating from the web interface appeared successful but the version stayed the same. Now updates from UI work correctly on all devices."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Pre-release versions shown as downgrade"})," \u2014 selecting a pre-release version (e.g. ",(0,i.jsx)(n.code,{children:"1.46.6rc"}),') in the update dialog incorrectly showed "Downgrade" instead of "Upgrade".']}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1465---2026-03-23",children:"[1.46.5] - 2026-03-23"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Domain-based routing not working with local DNS"})," \u2014 on routers running their own DNS (e.g. ",(0,i.jsx)(n.code,{children:"dnsmasq"})," on ",(0,i.jsx)(n.code,{children:"OpenWrt"}),"), B4 couldn't learn which IPs belong to routed domains. Now it works correctly regardless of where DNS is handled."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Routing stops working after toggling it off and on"})," \u2014 re-enabling routing on a set required waiting for fresh DNS traffic before it would take effect. Now B4 immediately resolves the domains and populates the routing table."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Tables monitor not detecting missing DNS routing rule"})," \u2014 the firewall monitor now checks that the OUTPUT chain DNS queue rule is in place, and triggers re-setup if it gets removed."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1460---2026-03-22",children:"[1.46.0] - 2026-03-22"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"MTProto proxy"})," \u2014 built-in Telegram ",(0,i.jsx)(n.code,{children:"MTProto"})," proxy with fake-TLS obfuscation. Telegram traffic is wrapped in TLS that looks like a normal HTTPS connection. Configure in Settings with a Fake SNI domain and generated secret, then paste the secret into Telegram's proxy settings."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"DC relay"})," \u2014 when Telegram DCs are blocked by IP, route through an external relay."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"SOCKS5 first-packet fragmentation"})," \u2014 the SOCKS5 proxy now splits the first data packet for matched connections, improving bypass for non-TLS protocols."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Non-TLS traffic handling"})," \u2014 learned IP matches now keep fragmentation and desync active even when traffic is not TLS."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1451---2026-03-21",children:"[1.45.1] - 2026-03-21"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"API documentation"})," \u2014 interactive Swagger UI is now available at ",(0,i.jsx)(n.code,{children:"/swagger/"})," to browse and test all REST API endpoints."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1450---2026-03-21",children:"[1.45.0] - 2026-03-21"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Traffic routing"})," \u2014 you can now route traffic for matched domains through a specific network interface. When a set matches a domain, B4 resolves its IPs and directs that traffic through the chosen output interface. Configure it in the ",(0,i.jsx)(n.code,{children:"Routing"})," tab when editing a set."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Position randomization"})," \u2014 SNI split position, OOB position, and TLS record position now support ranges. Each connection picks a random value within the range, making traffic harder for DPI to fingerprint."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Strategy randomization pool"})," \u2014 you can now select multiple splitting strategies and B4 will randomly pick one per connection. Constantly changing how packets are split confuses stateful DPI."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Combo/Disorder timing ranges"})," \u2014 First Segment Delay, Jitter Max, and Fakes Per Segment now support ranges. Each connection gets different timing and fake packet counts, preventing DPI from building a consistent traffic fingerprint."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Fake packets now match real connection fingerprint"})," \u2014 fake desync packets (RST, FIN, ACK) now preserve the original TCP options (timestamps, window scale, SACK) instead of stripping them. This prevents DPI from detecting fakes by comparing TCP header fingerprints."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Dynamic fake TTL"})," \u2014 fake packet TTL is now clamped to never exceed the real packet's TTL, preventing impossible TTL values that DPI systems use to identify forged packets."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Dual fake-packet evasion"})," \u2014 fake packets now use both corrupted checksums and dynamic TTL together, making them harder for DPI to accept while ensuring the real server drops them."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"TLS info shown on all packets"})," \u2014 domain name and TLS version now appear in logs for every packet in a connection, not just the first one."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Managing large domain/IP lists"}),' \u2014 added a bulk text editor for domains and IPs, and long lists now collapse with a "+N more" button.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Payload handling"})," \u2014 fixed incorrect filenames on upload/download and unnecessary loading errors when sets are disabled."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Syslog crashes B4 in Docker"})," \u2014 enabling syslog in a ",(0,i.jsx)(n.code,{children:"Docker"})," container caused B4 to crash-loop because the syslog socket doesn't exist. Now B4 logs a warning and continues without syslog."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Time zone not applying on routers"})," \u2014 setting a time zone in ",(0,i.jsx)(n.code,{children:"Settings"})," had no effect on some routers (e.g. ",(0,i.jsx)(n.code,{children:"Keenetic"}),") because the device lacked timezone data."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Log level resets to Info on restart"})," \u2014 changing the log verbosity in ",(0,i.jsx)(n.code,{children:"Settings"})," did not persist across service restarts."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Invalid TLS certificate crashes B4"})," \u2014 setting a wrong certificate or key path in ",(0,i.jsx)(n.code,{children:"Settings"})," caused B4 to crash on next restart. Now it logs a warning and falls back to ",(0,i.jsx)(n.code,{children:"HTTP"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"ASN data now persists on the router"})," \u2014 enriched ASN information is stored server-side, so it stays consistent across all browsers and devices."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Inline ASN enrichment"})," \u2014 you can now enrich destination IPs with ASN data directly from the connections table without opening the Add IP dialog."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1441---2026-03-15",children:"[1.44.1] - 2026-03-15"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Time zone setting"})," \u2014 log timestamps now use your system's local time by default instead of UTC. You can also pick a specific time zone in Settings > Logging."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Payload download not working"}),' \u2014 clicking "Download" on a captured payload in the web UI did nothing. Now downloads work correctly.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Uploaded payload filename"})," \u2014 when uploading a payload file, the saved name included the file extension twice (e.g. ",(0,i.jsx)(n.code,{children:"tls_payload_bin.bin"}),"). Now it saves correctly (e.g. ",(0,i.jsx)(n.code,{children:"tls_payload.bin"}),")."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Update from web UI not applying"}),' \u2014 clicking "Update" in the web interface would appear to run, but B4 would restart with the same version.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'DPI bypass not working with TLS version set to "Any"'}),' \u2014 when a set\'s TLS version filter was set to "Any", connections would break after the initial handshake. Selecting a specific version (1.2 or 1.3) worked fine. The issue was that encrypted data packets were being incorrectly processed as if they were new TLS handshakes.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"B4 crashing on some Xiaomi routers (BE7000, AX3200 and similar)"}),' \u2014 B4 would fail to start with a "Could not process rule" error because it incorrectly chose nftables on devices where nftables isn\'t fully supported. B4 now tests whether nftables actually works before using it, and automatically falls back to iptables-legacy when needed. (issues ',(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/132",children:"#132"})," ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/133",children:"#133"}),")"]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Firewall engine selector"}),' \u2014 a new "Firewall Engine" option in Settings lets you manually choose between ',(0,i.jsx)(n.code,{children:"nftables"}),", ",(0,i.jsx)(n.code,{children:"iptables"}),", or ",(0,i.jsx)(n.code,{children:"iptables-legacy"})," if auto-detection doesn't work for your device."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1430---2026-03-14",children:"[1.43.0] - 2026-03-14"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Language selection"})," \u2014 you can now switch the web interface language in Settings. English and Russian are available."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'Update stuck on "Waiting for service to restart"'})," \u2014 when login protection was enabled, the update process would get stuck polling forever after the service restarted."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Excessive logging under heavy traffic"})," \u2014 on busy routers, packet queue overflows could flood the log with repeated error messages, wasting CPU and potentially making the situation worse. These messages are now rate-limited."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"TLS version not shown for some UDP connections"})," \u2014 QUIC (UDP) connections would sometimes show a blank TLS version in the log and connections table, even though they always use TLS 1.3. Now all QUIC traffic correctly displays its TLS version."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1421---2026-03-13",children:"[1.42.1] - 2026-03-13"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"B4 not capturing traffic on older routers"})," \u2014 on some routers with older kernels (e.g. ASUS RT-AC68U), B4 would start but show no connections. The fix ensures B4 properly registers itself with the system's packet filtering, so traffic is captured without needing any workarounds."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Copy JSON not working on Safari/Mac"}),' \u2014 the "Copy JSON" button in Import/Export now works reliably on Safari and other browsers that restrict clipboard access.']}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1420---2026-03-11",children:"[1.42.0] - 2026-03-11"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Backup & Restore"})," \u2014 you can now download a full backup of your B4 configuration and restore it later. Go to Settings > Backup to download a ",(0,i.jsx)(n.code,{children:".tar.gz"})," file with your config, discovery history, detector history, device aliases, and captured payloads. To restore, just upload a previously downloaded backup and restart B4."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Web UI login protection"})," \u2014 you can now set a username and password to protect the B4 web interface. Useful when B4 is installed on a VPS or accessible from the internet. Set it up in Settings > Web Server > Authentication, or during installation. When enabled, a login page appears before you can access the dashboard."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"TLS version filter"})," \u2014 you can now assign a set to only handle TLS 1.2 or TLS 1.3 traffic. This is useful when the same domain uses different TLS versions on different devices (e.g., a smart TV using TLS 1.2 vs a browser using TLS 1.3) and each needs a different bypass strategy. Set the filter in ",(0,i.jsx)(n.code,{children:"Targets > TLS Version Filter"}),'. Leave it on "Any" to match all traffic as before.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"TLS version in Connections table"})," \u2014 the connections log now shows the detected TLS version (1.2 or 1.3) as a badge next to the domain name, so you can see which version each connection uses."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Discovery sets TLS filter automatically"})," \u2014 when you run Discovery with a specific TLS version (TLS 1.2 or TLS 1.3), the resulting set will automatically have the matching TLS filter applied."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Import/Export now shows a clear success message"})," \u2014 after importing a set configuration, a green banner confirms the import worked and reminds you to save. Previously, the page would silently switch to another tab, which was easy to miss."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1410---2026-03-09",children:"[1.41.0] - 2026-03-09"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"DPI Detector results are now saved"}),' \u2014 after running detection tests, your results are saved and shown in a "Previous Results" section. You can expand any past run to see full details, or delete individual entries.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"DPI Detector remembers your test selection"})," \u2014 the test toggles (DNS, Domains, TCP, SNI) are now remembered between visits."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Discovery results are now saved"}),' \u2014 when Discovery finishes testing a domain, the results are kept on the server. Refreshing the page or coming back later will show your previous results in a "Previous Results" section at the bottom.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Auto-reconnect to running Discovery"})," \u2014 if you refresh the page while Discovery is running, it automatically reconnects and shows progress. No more lost sessions."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Use strategies from past results"}),' \u2014 you can apply a working strategy from a previous Discovery run without re-testing. Just expand the domain and click "Use This Strategy".']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Re-test and manage history"}),' \u2014 each past result has a "Re-test" button to quickly run Discovery again for that domain. You can also delete individual results or clear all history.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Duplicate domain warning"})," \u2014 when adding a domain or IP to a set, B4 now warns you if it already exists in another set (including domains inside GeoSite categories). This helps avoid accidental overlaps between sets."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Wrong set assigned to traffic"}),' \u2014 in rare cases, traffic could be matched to the wrong set when multiple sets had overlapping IP ranges (e.g., a set with specific IPs and another with broad geo-IP categories like "cloudflare"). The most specific match now always wins. Also, subsequent TCP packets now correctly use the set that was previously identified by domain name, instead of falling back to a less accurate IP-only match.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'DNS check no longer gives false "poisoned" results'})," \u2014 if your router uses DoH, DNSCrypt, or another encrypted DNS, Discovery would incorrectly report DNS as poisoned (because IPs didn't match exactly). Now it actually checks whether the IPs work, not just whether they match."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'ISP block pages no longer count as "success"'})," \u2014 previously, if the ISP returned a block page (with valid HTML), Discovery could mistakenly think the bypass worked. Now it detects Russian ISP block pages and correctly marks them as failed."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Discovery hangs after detecting DNS poisoning"})," \u2014 when DNS bypass strategies (fragmented queries, alternative servers) got no response, the lookup had no timeout and would wait forever. Now properly times out after 10 seconds per attempt."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Discovery is much faster"})," \u2014 removed unnecessary DNS lookups on every test, added early exit when a strategy clearly doesn't work, and limited IP fallback retries. Failed presets now take ~5s instead of ~35s."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"IP-blocked domains detected and skipped"}),' \u2014 when a domain is completely blocked at the network level (like Instagram in some regions), Discovery now detects this early, shows a "Blocked" badge, and skips the extended search instead of wasting time testing 100+ strategies that can\'t work.']}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Clearer error messages"}),' \u2014 instead of raw technical errors like "context deadline exceeded", you now see human-readable messages like "connection timed out" or "connection reset by DPI/firewall".']}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Better firewall error messages"}),' \u2014 errors now show which rule failed and why, instead of just "exit status x".']}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1400---2026-03-08",children:"[1.40.0] - 2026-03-08"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Mass delete sets"}),' \u2014 you can now select multiple sets at once and delete them all together. Click the "Select" button in the toolbar, check the sets you want to remove, then click "Delete". Includes "Select All" for quick bulk cleanup.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Multidisorder mode"}),' \u2014 sends fake overlap packets before every real segment (not just the first one), flooding DPI with garbage data so it can\'t reassemble your traffic correctly. Enable "Fake Per Segment" in Combo or Disorder fragmentation settings and choose how many fakes to send per segment.']}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"New fake payload types"}),' \u2014 two new options in Faking settings: "All Zeros" (sends empty-looking data) and "Inverted Original" (sends a flipped copy of the real data). Some networks respond better to these than the default payloads.']}),"\n",(0,i.jsx)(n.li,{children:"ADDED: New Discovery presets for multidisorder mode \u2014 Discovery can now automatically test these new techniques when searching for the best bypass configuration."}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Upload GeoIP/GeoSite files"})," \u2014 you can now upload ",(0,i.jsx)(n.code,{children:".dat"}),' files directly from your computer using the "Upload" button in ',(0,i.jsx)(n.code,{children:"Settings > Geo Databases"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"TCP Port Filter"})," \u2014 B4 no longer only captures TCP port 443. You can now configure custom TCP ports per set (e.g., ",(0,i.jsx)(n.code,{children:"80,5222,8000-9000"}),") in the TCP settings tab, just like UDP. Port 443 is always included. Firewall rules, packet processing, and the monitor all update automatically \u2014 no restart needed. Useful for services like Telegram (port 5222), WhatsApp (5222-5223), Signal (4433), XMPP, and others that use non-443 TCP ports."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Main Set removed"}),' \u2014 there is no longer a special "main" set. All sets are now equal and independent. Your existing main set will be converted into a regular set automatically.']}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Connection Bytes Limits moved to Settings"})," \u2014 TCP and UDP connection bytes limits are now in ",(0,i.jsx)(n.code,{children:"Settings > Core > Queue Settings"})," instead of being tied to a specific set."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Device list sorting"})," \u2014 devices are now sorted alphabetically by name, with selected devices always shown at the top for easier access."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Set Import not working on Android"})," \u2014 pasting a set configuration from the clipboard was not possible on Android devices."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Set Import/Export"})," \u2014 the exported JSON is now much shorter and easier to read. Only settings you actually changed are shown; everything else is left out since it uses defaults. A ",(0,i.jsx)(n.code,{children:"b4_version"})," tag is included so you can tell which B4 version a shared set was made with."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Simpler import flow"}),' \u2014 pasting a set JSON now applies it immediately (no more forgetting to click "Apply"). Added Copy and Paste buttons for quick sharing.']}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Lower memory usage"})," \u2014 fixed several memory leaks and added automatic memory management."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Network interface not visible on MikroTik"})," \u2014 ",(0,i.jsx)(n.code,{children:"veth"})," interfaces (used by MikroTik containers) were incorrectly hidden. They now show up properly in the interface list."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1391---2026-03-02",children:"[1.39.1] - 2026-03-02"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"MSS Clamping"})," \u2014 forces smaller packet sizes at the firewall level so that blocked content (like YouTube on smart TVs) can load correctly. Two options: enable ",(0,i.jsx)(n.strong,{children:"globally"})," for all devices in ",(0,i.jsx)(n.code,{children:"Settings > Network > Global MSS Clamping"}),", or set a ",(0,i.jsx)(n.strong,{children:"per-device"})," size in the ",(0,i.jsx)(n.code,{children:"Settings > Device Filtering"})," table (MSS column). Changes apply instantly without restarting."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"DPI Detector"})," \u2014 a new page in the sidebar that checks whether your ISP is tampering with your internet traffic. It runs three quick tests: DNS spoofing, blocked website detection, and connection dropping. Helps you see what your ISP is actually doing before and after enabling B4."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"NAT Masquerade"})," \u2014 B4 can now set up NAT masquerade automatically when running inside containers (Docker, LXC, MikroTik CHR). No more manual scripts \u2014 just enable ",(0,i.jsx)(n.code,{children:"NAT Masquerade"})," in ",(0,i.jsx)(n.code,{children:"Settings > Feature Flags > Firewall Features"})," and optionally pick an output interface. Works with both ",(0,i.jsx)(n.code,{children:"iptables"})," and ",(0,i.jsx)(n.code,{children:"nftables"}),". Rules are monitored and auto-restored if they disappear. Also available via CLI: ",(0,i.jsx)(n.code,{children:"--masquerade"})," and ",(0,i.jsx)(n.code,{children:"--masquerade-interface"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Custom payloads ignored during Discovery"})," \u2014 selecting custom payloads no longer silently falls back to built-in ones like duckduckgo. Your custom payloads are now properly used in the discovered configuration."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Discovery results not working after adding"})," \u2014 configurations that passed during Discovery could fail when actually applied. The tested config now matches exactly what gets saved."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"TTL detection not working"})," \u2014 the optimal TTL search was not actually changing the fake packet's TTL, making all attempts look the same. Now correctly finds the minimum working TTL for your network."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Custom capture payloads lost when adding from Discovery"})," \u2014 configurations using captured payload files (e.g., from the ",(0,i.jsx)(n.code,{children:"Capture feature"}),") would lose the payload reference after being added, causing the bypass to fail. The payload type also didn't show correctly in the set editor until manually reselected. Both are now fixed."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Discovery Cache"})," \u2014 Discovery now remembers which bypass strategies worked before. When you run Discovery for a new domain, it tries previously successful configurations first, so you often get a working result quicker."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Multi-Domain Discovery"})," \u2014 you can now test multiple domains in a single Discovery run. Add domains or full URLs one by one (they appear as chips for easy management) and B4 will find the best bypass configuration for each one."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Smarter Discovery"})," \u2014 reworked strategy testing to use real-world technique combinations instead of testing individual tricks in isolation. If Discovery says a strategy works, it should actually work when you add it."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Smarter TTL in Discovery"})," \u2014 ",(0,i.jsx)(n.code,{children:"Discovery"})," finds the optimal Fake TTL for Combo configurations automatically by scanning through preset TTL values, and tests all faking strategies (including ",(0,i.jsx)(n.code,{children:"timestamp"}),") to pick the most reliable one."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Fullscreen Discovery Logs"})," \u2014 added a button to view Discovery logs in a large popup window, making long log lines much easier to read."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Set editor no longer jumps away after saving"}),' \u2014 clicking "Save" now keeps you on the same tab and scroll position instead of going back to the sets list. This can help to speed up testing specific configurations.']}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Auto-detect config file"})," \u2014 the ",(0,i.jsx)(n.code,{children:"--config"})," flag is no longer required. When omitted, B4 automatically looks for a config file in ",(0,i.jsx)(n.code,{children:"/etc/b4/"})," and ",(0,i.jsx)(n.code,{children:"/opt/etc/b4/"}),". If no config exists yet, B4 picks the best default location and creates one on first run."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: SOCKS5 updates (thanks @remmody ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/pull/64",children:"#PR64"}),")"]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Device Discovery"})," \u2014 now works on all routers. B4 reads the system ARP table instead of DHCP lease files, so devices show up regardless of your router brand (Keenetic, MikroTik, OpenWrt, Asus, etc.). Device hostnames are still picked up from DHCP when available."]}),"\n",(0,i.jsxs)(n.li,{children:["REMOVED: ",(0,i.jsx)(n.strong,{children:"Decoy SNI Domains"})," setting from Combo strategy \u2014 the decoy packet now uses the same fake payload configured in your Faking settings instead of a separate list of domain names."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1380---2026-02-27",children:"[1.38.0] - 2026-02-27"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.strong,{children:"Vendor Lookup is now optional"})," \u2014 the ~6MB device manufacturer database is no longer downloaded at startup. Enable it in ",(0,i.jsx)(n.code,{children:"Settings > Device Filtering > Vendor Lookup"})," if you want to see device brand names."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"TLS Version selector in Discovery"})," \u2014 you can now choose which TLS version (",(0,i.jsx)(n.code,{children:"Auto"})," / ",(0,i.jsx)(n.code,{children:"TLS 1.2"})," / ",(0,i.jsx)(n.code,{children:"TLS 1.3"}),") to use when probing."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"SOCKS5 Proxy"})," \u2014 B4 now includes a built-in SOCKS5 proxy server. Apps like browsers, curl, or torrent clients can route traffic through B4 without any system-wide setup. Enable it in ",(0,i.jsx)(n.code,{children:"Settings > Network Configuration > SOCKS5 Server"}),". Supports optional username/password authentication. (",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/pull/48",children:"#48"}),", thanks ",(0,i.jsx)(n.a,{href:"https://github.com/remmody",children:"@remmody"}),")"]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Docker support"})," \u2014 detects Docker environment and shows ",(0,i.jsx)(n.code,{children:"docker pull"})," instructions instead of the update button."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Settings page crash (white screen) in container environments"})," \u2014 network interface filtering is now container-aware, showing ",(0,i.jsx)(n.code,{children:"veth"})," and other container interfaces when running inside Docker/MikroTik. (",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/44",children:"#44"}),", thanks ",(0,i.jsx)(n.a,{href:"https://github.com/kakosmakos",children:"@kakosmakos"}),")"]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:'DNS check incorrectly reporting "DNS poisoned"'})," when everything is actually fine. The check could time out before even trying the system resolver, producing a false alarm. Also improved handling of CDN domains (YouTube, Google, etc.) where different resolvers return different but equally valid IPs. (",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/issues/46",children:"#46"}),")"]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1370---2026-02-26",children:"[1.37.0] - 2026-02-26"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"HTTPS/TLS support"})," for the web interface (",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/pull/40",children:"#40"}),", thanks ",(0,i.jsx)(n.a,{href:"https://github.com/Shiperoid",children:"@Shiperoid"}),"). Configure in Web UI (",(0,i.jsx)(n.code,{children:"Settings > Network > Web Server"}),") or in the config JSON. The installer auto-detects router certificates on ",(0,i.jsx)(n.strong,{children:"OpenWrt"})," and ",(0,i.jsx)(n.strong,{children:"Asus Merlin"})," and offers to enable HTTPS during installation."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Reset Statistics"})," button on the dashboard \u2014 clears all counters without restarting the service. Replaces the redundant restart button (still available in ",(0,i.jsx)(n.code,{children:"Settings > Core Controls"}),")."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.strong,{children:"Geo database download failing with 500 error"})," on routers with limited storage. Improved disk space handling and error reporting."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1360---2026-02-16",children:"[1.36.0] - 2026-02-16"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Connections Table"})," \u2014 moved the streaming/paused control from the top control bar to a floating play/stop button in the bottom-right corner of the table for a cleaner UI."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Set Editor Redesign"})," \u2014 simplified the tab layout from 7 tabs down to 5. ",(0,i.jsx)(n.code,{children:"Fragmentation"})," and ",(0,i.jsx)(n.code,{children:"Faking"})," tabs are now part of the ",(0,i.jsx)(n.code,{children:"TCP"})," section where they belong, since both techniques operate on TCP traffic.","\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"TCP"})," tab now has 3 inner tabs: ",(0,i.jsx)(n.code,{children:"General"})," (connection limits, timing, duplication), ",(0,i.jsx)(n.code,{children:"Splitting"})," (all packet splitting strategies), and ",(0,i.jsx)(n.code,{children:"Faking"})," (all evasion techniques in one place)."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.code,{children:"Faking"})," groups related settings into collapsible sections (Fake SNI, SYN Fake, Desync, Window Manipulation, Incoming Response Bypass, ClientHello Mutation) \u2014 each section shows its current status at a glance so you can see what's active without opening it."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.code,{children:"UDP"}),", ",(0,i.jsx)(n.code,{children:"DNS"}),", ",(0,i.jsx)(n.code,{children:"Targets"}),", and ",(0,i.jsx)(n.code,{children:"Import/Export"})," tabs remain unchanged."]}),"\n"]}),"\n"]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1352---2026-02-16",children:"[1.35.2] - 2026-02-16"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: All changes being lost and tabs resetting when creating a new ",(0,i.jsx)(n.code,{children:"set"}),". Any edit (switching tabs, adding categories, changing settings) could be randomly undone."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: Custom TCP window values (add/remove) not being saved when using ",(0,i.jsx)(n.code,{children:"Oscillate"})," or ",(0,i.jsx)(n.code,{children:"Random"})," window modes."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"Packet Duplication"})," connections showing incorrect data in the connections table."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1350---2026-02-15",children:"[1.35.0] - 2026-02-15"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.strong,{children:"Geo Settings"})," - ",(0,i.jsx)(n.code,{children:"GeoSite"})," and ",(0,i.jsx)(n.code,{children:"GeoIP"})," databases can now be downloaded independently from different sources. You no longer need both files \u2014 pick only what you need. Added ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4geoip",children:"b4geoip"})," as a built-in source option."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Packet Duplication"})," - bypass ISP throttling that works by randomly dropping packets to specific IP ranges (e.g. Telegram subnets). When enabled, B4 sends multiple copies of each outgoing packet so your connection survives even when the ISP drops some of them. Create a separate set with the target IPs, go to TCP settings, and enable Packet Duplication. Set the copy count (2-5 is usually enough). Note: this replaces all other DPI bypass for that set and uses extra bandwidth."]}),"\n",(0,i.jsx)(n.li,{children:'FIXED: Some connections to matched domains intermittently showing as "not matched". This was caused by an internal cache issue and learned IP associations being lost when saving settings or updating geo databases.'}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1340---2026-02-10",children:"[1.34.0] - 2026-02-10"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Randomized Segment 2 Delay"})," - instead of a fixed delay between TCP/UDP segments, you can now set a min\u2013max range. Each packet picks a random delay within your range, making your traffic look more natural and harder for DPI to fingerprint. If both values are the same, it works exactly like before."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Redesigned ",(0,i.jsx)(n.code,{children:"Dashboard"})," - the dashboard now shows what actually matters:","\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Device Activity"})," - see which devices on your network are connecting to which domains (e.g., your PlayStation, iPhone, or laptop), with connection counts per domain."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Domains Not In Any Set"}),' - quickly spot domains that aren\'t covered by any bypass set yet, with one-click "Add to set" buttons.']}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Bypass stats"})," - at a glance, see total connections, how many are being bypassed, and current throughput."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Active Sets"})," overview with clickable chips to jump to set configuration."]}),"\n"]}),"\n"]}),"\n",(0,i.jsx)(n.li,{children:"ADDED: The dashboard now tracks all network traffic, not just bypassed connections. This means device activity and domain lists populate even before you configure any sets."}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.code,{children:"Set editor"})," now opens as a full page instead of a popup window, giving you much more space to work with when configuring your bypass sets."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"TCP Timestamp"})," faking strategy - a new way to make fake packets look wrong to the real server (so it ignores them) while still fooling DPI. Instead of using a low TTL or wrong sequence number, B4 sends fake packets with an outdated timestamp. Inspired by the ",(0,i.jsx)(n.a,{href:"https://github.com/Waujito/youtubeUnblock",children:"youtubeUnblock"})," project. Select ",(0,i.jsx)(n.code,{children:"TCP Timestamp"})," in the Faking strategy dropdown to use it."]}),"\n",(0,i.jsx)(n.li,{children:'FIXED: UDP/QUIC traffic stopping to match after some time. B4 now keeps server-to-domain associations active as long as traffic is flowing, preventing the "works at first, then stops" issue.'}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: Rewrite TLS ",(0,i.jsx)(n.code,{children:"ClientHello"})," payload generation."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1320---2026-01-19",children:"[1.32.0] - 2026-01-19"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: Validation tries setting in ",(0,i.jsx)(n.code,{children:"Discovery"})," - require multiple successful connections before accepting a configuration as reliable (default: 1, configurable 1-5). Helps filter out unstable bypass methods."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: UDP/QUIC filtering now correctly matches packets from domain-specified targets. When you add a domain like ",(0,i.jsx)(n.code,{children:"youtube.com"}),", B4 now tracks which IP addresses belong to that domain and properly handles all UDP traffic to those IPs, not just the initial connection."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: Connection logs now show domain names for ",(0,i.jsx)(n.code,{children:"QUIC"})," traffic even when not actively filtering those connections."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: UDP port filtering incorrectly matching unrelated traffic. Previously, specifying port 443 for one service could accidentally match all UDP traffic on port 443, including other services."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1312---2026-01-16",children:"[1.31.2] - 2026-01-16"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: Custom payload files not working in ",(0,i.jsx)(n.code,{children:"Discovery"})," feature - old configurations with relative paths like ",(0,i.jsx)(n.code,{children:"captures/payload.bin"})," now work correctly."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1310---2026-01-11",children:"[1.31.0] - 2026-01-11"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"ADDED: TCP MD5 option support."}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: TCP MD5 preset in Discovery - automatically tests ",(0,i.jsx)(n.code,{children:"TCP MD5"})," bypass strategy during configuration discovery."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"iptables"})," multiport module support - improves firewall rule efficiency when filtering multiple ports. The installer now detects and uses the ",(0,i.jsx)(n.code,{children:"multiport"})," extension when available."]}),"\n",(0,i.jsx)(n.li,{children:"IMPROVED: Import/Export now automatically migrates old set configurations (pre-v1.29) to the current format when importing."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1301---2026-01-02",children:"[1.30.1] - 2026-01-02"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"FIXED: White screen when importing old set configurations (sets saved before v1.29 now automatically convert to current format)."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1300---2026-01-02",children:"[1.30.0] - 2026-01-02"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: Custom payload support in ",(0,i.jsx)(n.code,{children:"Discovery"})," - test bypass strategies using your own captured TLS payloads instead of built-in defaults."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: Crash during ",(0,i.jsx)(n.code,{children:"Discovery"}),' when checking status (e.g. clicking "Create Set" or refreshing page while discovery is running. Reported by ',(0,i.jsx)(n.code,{children:"Andrew B."}),")."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1291---2026-01-01",children:"[1.29.1] - 2026-01-01"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: Incoming response bypass - defeats TSPU throttling that blocks downloads after ~15KB. I nmost cases - select TCP incoming ",(0,i.jsx)(n.code,{children:"fake"})," for all strategies."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Discovery progress bar sometimes showing over 100%."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1281---2025-12-30",children:"[1.28.1] - 2025-12-30"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: Web server bind address setting - control which network interface the web UI listens on (e.g., ",(0,i.jsx)(n.code,{children:"127.0.0.1"})," for localhost-only access, ",(0,i.jsx)(n.code,{children:"0.0.0.0"})," for all interfaces). Supports ",(0,i.jsx)(n.code,{children:"IPv6"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Added ",(0,i.jsx)(n.code,{children:"Skip DNS"})," toggle in ",(0,i.jsx)(n.code,{children:"Discovery"})," - useful when you know DNS isn't blocked and want faster results."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Support for ",(0,i.jsx)(n.code,{children:"MIPS"})," devices with soft float."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Post-ClientHello RST injection - sends fake connection reset after the initial handshake to confuse DPI systems that track connection state. Enable with ",(0,i.jsx)(n.code,{children:"Post-ClientHello RST"})," toggle in TCP Desync Set settings."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.code,{children:"Discovery"})," now finds the optimal TTL for a specific network, instead of using a fixed value."]}),"\n",(0,i.jsx)(n.li,{children:"IMPROVED: Removed DPI fingerprinting phase from discovery - it was slow and unreliable. Discovery now starts testing bypass strategies immediately, making the process faster."}),"\n",(0,i.jsx)(n.li,{children:"IMPROVED: Shutdown behavior - B4 now waits up to 5 seconds for in-flight packet operations to complete gracefully before terminating."}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: DPI bypass not working for LAN devices on routers using ",(0,i.jsx)(n.code,{children:"nftables"})," (e.g., OpenWrt with fw4 firewall)."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: TCP ",(0,i.jsx)(n.code,{children:"desync"})," bypass methods (",(0,i.jsx)(n.code,{children:"rst"}),", ",(0,i.jsx)(n.code,{children:"fin"}),", ",(0,i.jsx)(n.code,{children:"ack"}),", ",(0,i.jsx)(n.code,{children:"combo"}),", ",(0,i.jsx)(n.code,{children:"full"})," modes) were sending malformed packets, causing them to fail or be ignored. Affects both ",(0,i.jsx)(n.code,{children:"IPv4"})," and ",(0,i.jsx)(n.code,{children:"IPv6"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: Restored missing ",(0,i.jsx)(n.code,{children:"Packet Mark"})," setting in Network Configuration - allows customizing the firewall mark used for traffic routing."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Potential connection hangs caused by packets getting stuck in the processing queue without verdicts being set."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Memory leak where packet injection operations could continue running after service shutdown, potentially causing crashes or instability during restart."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Race condition in network interface name caching that could cause incorrect packet filtering decisions on multi-interface systems."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Fragmented IP packets now bypass B4 processing entirely to prevent incomplete packet inspection and potential protocol violations."}),"\n",(0,i.jsxs)(n.li,{children:["REMOVED: Fragmentation strategy ",(0,i.jsx)(n.code,{children:"overlap"})," \u2014 functionality merged into ",(0,i.jsx)(n.code,{children:"combo"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: Fragmentation ",(0,i.jsx)(n.code,{children:"combo"})," now supports decoy packets. When ",(0,i.jsx)(n.code,{children:"enabled"}),", B4 sends a fake ",(0,i.jsx)(n.code,{children:"ClientHello"})," with a whitelisted domain (e.g., ya.ru, vk.com) before sending the real fragmented request. Can be found in Fragmentaiton Tab set settings."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1272---2025-12-27",children:"[1.27.2] - 2025-12-27"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: Adding multiple services with many UDP ports (Discord, WhatsApp, etc.) could cause ",(0,i.jsx)(n.code,{children:"iptables"})," firewall rules to fail, preventing B4 from starting or restarting properly."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1271---2025-12-27",children:"[1.27.1] - 2025-12-27"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"FIXED: DNS poisoning detection should correctly compare IP lists from system resolver vs encrypted DNS, even when IPs are returned in different order."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: UDP/QUIC traffic for IP-matched services (like YouTube, Google) could be incorrectly handled when another set had port-based filtering, potentially breaking video streaming."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: A bug when downgrading to a previous version from the web UI would report success but stay on the current version."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1270---2025-12-27",children:"[1.27.0] - 2025-12-27"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"Discovery Logs"})," panel shows real-time progress during configuration discovery."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Smart CDN detection for ",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/blob/main/src/discovery/dns.json",children:"20+ major services"})," (",(0,i.jsx)(n.code,{children:"Instagram"}),", ",(0,i.jsx)(n.code,{children:"Facebook"}),", ",(0,i.jsx)(n.code,{children:"YouTube"}),", ",(0,i.jsx)(n.code,{children:"Twitter/X"}),", Teleg",(0,i.jsx)(n.code,{children:"r"}),"am, ",(0,i.jsx)(n.code,{children:"Discord"}),", ",(0,i.jsx)(n.code,{children:"TikTok"}),", ",(0,i.jsx)(n.code,{children:"Netflix"}),", ",(0,i.jsx)(n.code,{children:"Spotify"}),", etc). Discovery now uses full IP ranges instead of single DNS results. ",(0,i.jsx)(n.a,{href:"https://github.com/daniellavrushin/b4/blob/main/src/discovery/dns.json",children:"View supported services"})]}),"\n",(0,i.jsx)(n.li,{children:"ADDED: Automatic DNS bypass for CDN services. When geo-blocked IPs are detected, B4 uses fragmented DNS queries to external resolvers to find working servers."}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.code,{children:"Discovery"})," Logs panel now shows more detailed real-time progress during DPI fingerprinting, DNS checks, and strategy testing."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.code,{children:"Discovery"})," now uses DNS-over-HTTPS (encrypted DNS via ",(0,i.jsx)(n.code,{children:"Google"}),"/",(0,i.jsx)(n.code,{children:"Quad9"}),"/",(0,i.jsx)(n.code,{children:"Cloudflare"}),") to detect when your ISP returns fake IP addresses for blocked sites. When DNS poisoning is detected, B4 connects directly to the real server to continue testing bypass strategies."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"QUIC"})," traffic (used by ",(0,i.jsx)(n.code,{children:"YouTube"}),", ",(0,i.jsx)(n.code,{children:"Google"}),", and many modern sites) was ignored when custom UDP ports were configured for any target set. Now UDP port ",(0,i.jsx)(n.code,{children:"443"})," is always monitored regardless of other port settings."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: UDP traffic to specific IPs now correctly uses the set that defines both the IP and port filter together, instead of being handled by a different set that only matches the port."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1263---2025-12-26",children:"[1.26.3] - 2025-12-26"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: UDP port ranges (e.g., ",(0,i.jsx)(n.code,{children:"50000-50032"}),") now work correctly on both ",(0,i.jsx)(n.code,{children:"iptables"})," and ",(0,i.jsx)(n.code,{children:"nftables"})," systems. Previously, port ranges could cause startup failures on older devices."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: Paste multiple domains at once when adding to ",(0,i.jsx)(n.code,{children:"Targets"})," (separated by spaces, commas, or pipes)."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1262---2025-12-25",children:"[1.26.2] - 2025-12-25"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: Normalize UDP port filter format by replacing dashes with colons before creating UDP rules in the ",(0,i.jsx)(n.code,{children:"iptables"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"Discovery"}),' now detects when DPI blocks downloads mid-transfer (e.g., cuts connection after 16KB), potentially preventing false "success" reports.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"Payload"}),' capture always showing "timeout" even when visiting the target site correctly.']}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1261---2025-12-24",children:"[1.26.1] - 2025-12-24"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Upload Custom Payloads"})," - upload your own binary payload files instead of capturing from live traffic (avilable in the ",(0,i.jsx)(n.code,{children:"Settings"})," -> ",(0,i.jsx)(n.code,{children:"Capture"})," tab)."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.strong,{children:"Use Captured Payloads"})," - new ",(0,i.jsx)(n.code,{children:"My own Payload file"})," option in ",(0,i.jsx)(n.code,{children:"Faking"})," settings lets you use previously captured or uploaded payload binaries."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: When adding a configuration from ",(0,i.jsx)(n.code,{children:"Discovery"}),", B4 automatically finds and includes matching geosite categories (e.g., discovering ",(0,i.jsx)(n.code,{children:"youtube.com"})," will also add the ",(0,i.jsx)(n.code,{children:"youtube"})," category with related domains from it)."]}),"\n",(0,i.jsx)(n.li,{children:"ADDED: Custom UDP port filtering - configure specific ports per set using the UDP port filter option, and B4 will automatically listen only on those ports."}),"\n",(0,i.jsx)(n.li,{children:"ADDED: Live firewall rule updates - changing UDP ports, connection limits, or other core settings in the web UI now takes effect immediately without restarting the service."}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Discovery now accepts full URLs. Paste a complete URL like ",(0,i.jsx)(n.code,{children:"https://youtube.com/watch?v=xyz"})," or ",(0,i.jsx)(n.code,{children:"https://cdn.example.com/large-file.js"})," instead of just a domain name."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Sequence Overlap Pattern - ",(0,i.jsx)(n.code,{children:"seqovl"}),". Configure a custom byte pattern in ",(0,i.jsx)(n.code,{children:"Fragmentation"})," settings that gets mixed into TCP segments to confuse deep packet inspection systems while your real data reaches the server intact. Works with ",(0,i.jsx)(n.code,{children:"disorder"})," and ",(0,i.jsx)(n.code,{children:"combo"})," strategies."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: UDP traffic now only listens on port ",(0,i.jsx)(n.code,{children:"443"})," (QUIC) by default instead of all UDP ports, reducing unnecessary packet processing."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: Device names in Connections tab now only appear when ",(0,i.jsx)(n.code,{children:"Device Filtering"})," is enabled in ",(0,i.jsx)(n.code,{children:"Settings"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: B4 no longer crashes on startup when geodat files (",(0,i.jsx)(n.code,{children:"geosite.dat"}),"/",(0,i.jsx)(n.code,{children:"geoip.dat"}),") were manually deleted."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Re-downloading geodat files now properly reloads all domain and IP targets without requiring a service restart."}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"IPv6"})," bypass settings now work correctly - disabling IPv6 in config actually disables IPv6 packet processing."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1254---2025-12-21",children:"[1.25.4] - 2025-12-21"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: installer failing to download on ",(0,i.jsx)(n.code,{children:"OpenWRT"})," devices - improved compatibility with minimal ",(0,i.jsx)(n.code,{children:"BusyBox"})," environments."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: repeated kernel module errors flooding syslog on ",(0,i.jsx)(n.code,{children:"OpenWRT"})," routers."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1253---2025-12-21",children:"[1.25.3] - 2025-12-21"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"nftables"})," forwarded traffic not working on ",(0,i.jsx)(n.code,{children:"OpenWRT"})," - changed hook from ",(0,i.jsx)(n.code,{children:"POSTROUTING"})," to ",(0,i.jsx)(n.code,{children:"FORWARD"})," to capture packets before NAT."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1252---2025-12-19",children:"[1.25.2] - 2025-12-19"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: add validation for fake SNI bounds and fallback to TCP fragments in ",(0,i.jsx)(n.code,{children:"overlap"})," handling."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: crash on 32-bit ARM devices caused by uint32 to int overflow in ",(0,i.jsx)(n.code,{children:"overlap"})," fragment handling."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1250---2025-12-19",children:"[1.25.0] - 2025-12-19"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["REMOVED: ",(0,i.jsx)(n.code,{children:"--skip-local-traffic"})," as it did actually nothing causing connection issues and solving real problem."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Device names in ",(0,i.jsx)(n.code,{children:"Connections"})," - identify which device (phone, laptop, etc.) is making each connection, with optional per-device filtering (whitelist/blacklist) in ",(0,i.jsx)(n.code,{children:"Settings"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: DHCP Device names:","\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Device Names"}),": See which device (phone, laptop, TV) is making each connection instead of cryptic IP addresses"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Device Filtering"}),": Choose which devices on your network should use DPI bypass (whitelist or blacklist mode)"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Custom Names"}),": Give your devices friendly names in ",(0,i.jsx)(n.code,{children:"Settings \u2192 Devices"})]}),"\n"]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Advanced ",(0,i.jsx)(n.code,{children:"Connections"})," table filtering:","\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["Combine filters with ",(0,i.jsx)(n.code,{children:"+"})," (e.g., ",(0,i.jsx)(n.code,{children:"tcp+youtube"}),")"]}),"\n",(0,i.jsxs)(n.li,{children:["Exclude with ",(0,i.jsx)(n.code,{children:"!"})," prefix (e.g., ",(0,i.jsx)(n.code,{children:"!google"})," or ",(0,i.jsx)(n.code,{children:"tcp+!udp"}),")"]}),"\n",(0,i.jsxs)(n.li,{children:["Field-specific filters: ",(0,i.jsx)(n.code,{children:"domain:"}),", ",(0,i.jsx)(n.code,{children:"asn:"}),", ",(0,i.jsx)(n.code,{children:"device:"})," (e.g., ",(0,i.jsx)(n.code,{children:"domain:youtube+!asn:cloudflare"}),")"]}),"\n"]}),"\n"]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1240---2025-12-17",children:"[1.24.0] - 2025-12-17"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"--skip-local-traffic"})," option to exclude router-originated traffic from processing, enabling compatibility with transparent proxies (Xray, Clash, Sing-Box etc.) running on the same device. By default is ",(0,i.jsx)(n.code,{children:"on"}),". Can be found in ",(0,i.jsx)(n.code,{children:"Core"})," settings. Requires service restart when changing."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Network interface filtering \u2014 optionally restrict B4 to specific interfaces (e.g., ",(0,i.jsx)(n.code,{children:"eth0"}),", ",(0,i.jsx)(n.code,{children:"tun0"}),"). Empty selection = all interfaces. Configurable via UI without service restart (can be found in ",(0,i.jsx)(n.code,{children:"Core"})," settings)."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ensure only ",(0,i.jsx)(n.code,{children:"Panic"})," errors are logged into errors.log."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1231---2025-12-16",children:"[1.23.1] - 2025-12-16"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: crash in ",(0,i.jsx)(n.code,{children:"overlap"})," fragmentation strategy when SNI extends beyond payload bounds (index out of range panic)."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"panic"})," errors not being captured to ",(0,i.jsx)(n.code,{children:"errors.log"})," file."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1230---2025-12-16",children:"[1.23.0] - 2025-12-16"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: dd error logging functionality with configurable error log file (default is ",(0,i.jsx)(n.code,{children:"/var/log/b4/errors.log"}),") for crash diagnostics."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"ASN"})," filtering in ",(0,i.jsx)(n.code,{children:"Connections"})," table - filter by ASN name globally or with ",(0,i.jsx)(n.code,{children:"asn:"})," field filter."]}),"\n",(0,i.jsx)(n.li,{children:"IMPROVED: prevent memory leaks in UI."}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: subordinate sets cannot exceed main set's TCP/UDP ",(0,i.jsx)(n.code,{children:"connection byte limits"})," both iun UI and backend."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1221---2025-12-09",children:"[1.22.1] - 2025-12-09"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"Discovery"}),' UI showing "0 of 0 checks" and "NaN%" during DNS detection phase.']}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"Discovery"}),' completing too fast causing "Failed to fetch discovery status" error.']}),"\n",(0,i.jsx)(n.li,{children:"FIXED: DNS poisoning detection falsely triggering for CDN domains by comparing IP addresses literally instead of testing actual connectivity."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1220---2025-12-08",children:"[1.22.0] - 2025-12-08"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"DNS Redirect"})," - bypass ISP DNS poisoning by transparently rewriting queries to clean resolvers. Available at set level, allowing per-domain DNS redirect control."]}),"\n",(0,i.jsx)(n.li,{children:"ADDED: DNS discovery and configuration management with support for custom DNS servers."}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"Enter"})," hotkey to start discovery. (",(0,i.jsx)(n.a,{href:"https://github.com/DanielLavrushin/b4/pull/5",children:"#5"}),")."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: enhance ",(0,i.jsx)(n.code,{children:"Discovery"})," fragmentation configurations and add new presets for combo and disorder strategies."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: backup handling for existing binaries during B4 installation (",(0,i.jsx)(n.code,{children:"installer.sh"}),")."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1211---2025-12-07",children:"[1.21.1] - 2025-12-07"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: TCP ",(0,i.jsx)(n.code,{children:"SYN Fake TTL"})," slider option."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: SYN fake packets sent with full TTL when using non-TTL faking strategies (randseq/pastseq), causing fake packets to reach servers and break handshakes."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Config validation incorrectly comparing set ConnBytesLimit against stale MainSet defaults instead of loaded JSON values."}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: UI bug when ui crashes on creating a set with ",(0,i.jsx)(n.code,{children:"Overlap"})," fragmentation option."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1210---2025-12-07",children:"[1.21.0] - 2025-12-07"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsx)(n.p,{children:"FIXED: Slow set save operations - improve performance."}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:["ADDED: New ",(0,i.jsx)(n.code,{children:"FRAG"})," strategies designed for modern DPI (TSPU):"]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.code,{children:"Combo"})," (recommended) - multi-technique: first-byte delay + extension split + SNI split + disorder"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.code,{children:"Disorder"})," - sends real segments out-of-order with timing jitter, no fake packets"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.code,{children:"Overlap"})," - overlapping TCP segments where second overwrites first (RFC 793 behavior)"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.code,{children:"Extension Split"})," - splits TLS ClientHello within extensions array before SNI"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.code,{children:"First-Byte Desync"})," - sends 1 byte, delays, sends rest (exploits DPI timeouts)"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.code,{children:"Hybrid"})," - evasion strategy combining desync, fake SNI, and disorder techniques"]}),"\n"]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsx)(n.p,{children:"IMPROVED: Skip private destination IP packets processing."}),"\n"]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1203---2025-12-03",children:"[1.20.3] - 2025-12-03"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"Discovery"})," false positives - now detects mid-transfer DPI blocking (throttling, stalls, resource blocking) instead of trusting initial HTTP 200."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"Discovery"})," network baseline - measures reference domain speed first, requires target to achieve 4KB+ downloaded at 30%+ of baseline speed. Configurable reference domain in ",(0,i.jsx)(n.code,{children:"Settings"})," \u2192 ",(0,i.jsx)(n.code,{children:"Discovery"})," (default: yandex.ru)."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"Discovery"})," binary search optimization for ",(0,i.jsx)(n.code,{children:"TTL"})," and fragmentation position parameters - reduces Phase 2 tests from ~50 to ~15 while finding optimal values. Uses fingerprint hints when available."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.code,{children:"Sets"})," editor save button now shows immediate loading feedback with spinner and disabled state to prevent double-submit during slow saves."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.code,{children:"Sets"})," manager now supports drag-and-drop reordering instead of up/down arrows."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1201---2025-12-03",children:"[1.20.1] - 2025-12-03"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"FIXED: IPv4 UDP fragmentation using incorrect fragment offset encoding (wrong bit shifts corrupted offset field)."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: IPv6 IP-level fragmentation not adjusting split position relative to IP payload, causing incorrect fragment boundaries."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: IP-level fragmentation (IPv4/IPv6) ignoring Smart SNI Split option - now uses middle_sni when enabled."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: QUIC fragmentation (IPv4/IPv6) had inverted ReverseOrder logic - was sending in reverse when disabled and normal when enabled."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: OOB fragmentation strategy was sending OOB byte in real packet that reached server, breaking TLS handshake. Now sends OOB as fake packet with low TTL/hop limit that dies in transit - DPI sees garbage, server receives clean data."}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"Discovery"})," service testing all presets with single hardcoded fake SNI payload - if user's DPI only responds to alternate payload, most strategies would incorrectly fail."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: OOB seg2 sequence number calculation was incorrect (added +1 for fake byte), causing TCP reassembly failures."}),"\n",(0,i.jsx)(n.li,{children:"ADDED: QUIC SNI-aware fragmentation - fragments now split at the actual SNI position within encrypted QUIC payloads, defeating DPI systems that decrypt QUIC Initial packets to inspect SNI."}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: OOB now supports ",(0,i.jsx)(n.code,{children:"tcp_check"})," and ",(0,i.jsx)(n.code,{children:"md5sum"})," faking strategies for checksum corruption fallback when TTL-based faking is unreliable."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: [EXPERIMENTAL] ",(0,i.jsx)(n.code,{children:"DPI Fingerprinting"})," in Discovery - attempts to identify DPI type and blocking method before testing, prioritizing likely-effective strategies based on failure mode analysis. May produce inaccurate results; falls back automatically to full preset scan when fingerprint is inconclusive."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Multiple built-in fake SNI payloads (",(0,i.jsx)(n.code,{children:"FakeSNI1"}),": google (default, AKA classic), ",(0,i.jsx)(n.code,{children:"FakeSNI2"}),": duckduckgo) - different DPI systems respond to different payloads."]}),"\n",(0,i.jsx)(n.li,{children:"ADDED: Discovery now auto-detects working payload early and applies it to all subsequent tests; if neither works initially, tests both per-strategy and learns dynamically."}),"\n",(0,i.jsx)(n.li,{children:"ADDED: User-selectable fake payload type in Faking settings (Random, Custom, or built-in presets)."}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.code,{children:"Fragmentation"}),' UI - renamed "Middle SNI" to "Smart SNI Split", added visual packet diagram, moved manual position to collapsible advanced section.']}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1200---2025-12-03",children:"[1.20.0] - 2025-12-03"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"FIXED: IPv4 UDP fragmentation using incorrect fragment offset encoding (wrong bit shifts corrupted offset field)."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: IPv6 IP-level fragmentation not adjusting split position relative to IP payload, causing incorrect fragment boundaries."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: IP-level fragmentation (IPv4/IPv6) ignoring Smart SNI Split option - now uses middle_sni when enabled."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: QUIC fragmentation (IPv4/IPv6) had inverted ReverseOrder logic - was sending in reverse when disabled and normal when enabled."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: OOB fragmentation strategy was sending OOB byte in real packet that reached server, breaking TLS handshake. Now sends OOB as fake packet with low TTL/hop limit that dies in transit - DPI sees garbage, server receives clean data."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: OOB seg2 sequence number calculation was incorrect (added +1 for fake byte), causing TCP reassembly failures."}),"\n",(0,i.jsx)(n.li,{children:"ADDED: QUIC SNI-aware fragmentation - fragments now split at the actual SNI position within encrypted QUIC payloads, defeating DPI systems that decrypt QUIC Initial packets to inspect SNI."}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: OOB now supports ",(0,i.jsx)(n.code,{children:"tcp_check"})," and ",(0,i.jsx)(n.code,{children:"md5sum"})," faking strategies for checksum corruption fallback when TTL-based faking is unreliable."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.code,{children:"Fragmentation"}),' UI - renamed "Middle SNI" to "Smart SNI Split", added visual packet diagram, moved manual position to collapsible advanced section.']}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1191---2025-12-01",children:"[1.19.1] - 2025-12-01"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"FIXED: New sets with geosite/geoip categories not matching traffic until service restart."}),"\n",(0,i.jsx)(n.li,{children:"IMPROVED: Discovery service now tests ~150+ configurations."}),"\n",(0,i.jsx)(n.li,{children:"CHANGED: Replace Fragmentation SNI split position Slider field with textbox number field."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1190---2025-11-29",children:"[1.19.0] - 2025-11-29"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: Filter for configuration sets - search by ",(0,i.jsx)(n.code,{children:"name"}),", ",(0,i.jsx)(n.code,{children:"SNI"})," domains, ",(0,i.jsx)(n.code,{children:"geosite"})," categories, or ",(0,i.jsx)(n.code,{children:"geoip"})," categories."]}),"\n",(0,i.jsx)(n.li,{children:"ADDED: Compare sets feature - side-by-side diff view showing differences between two configuration sets, grouped by section (TCP, UDP, Fragmentation, Faking, Targets)."}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"Discovery"})," now names new sets after the preset configuration (e.g., ",(0,i.jsx)(n.code,{children:"tcp-frag-rev-fake"}),") instead of the domain."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"Discovery"})," detects similar existing sets and offers to add the domain to an existing set instead of creating a new one."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"Discovery"})," short-circuits when baseline succeeds - skips optimization phases if no DPI is detected."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"Discovery"})," shows results progressively as tests complete - users can see working configurations and apply them without waiting for the full scan to finish."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Dedicated Sets API endpoints (",(0,i.jsx)(n.code,{children:"/api/sets"}),") for CRUD operations - create, update, delete, and reorder sets independently."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Settings tab navigation losing selected tab on page refresh."}),"\n",(0,i.jsx)(n.li,{children:"CHANGED: New configuration sets are now added to the top of the list instead of the bottom."}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.code,{children:"Discovery"})," configuration refactoring component."]}),"\n",(0,i.jsx)(n.li,{children:"CHANGED: Configuration Sets promoted from Settings submenu to top-level navigation item."}),"\n",(0,i.jsx)(n.li,{children:'CHANGED: Set operations (create, edit, delete, duplicate, reorder, enable/disable) now save immediately via API instead of requiring manual "Save Changes".'}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1185---2025-11-27",children:"[1.18.5] - 2025-11-27"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"FIXED: revert back the Fake SNI payload for improved compatibility."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1184---2025-11-25",children:"[1.18.4] - 2025-11-25"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["CHANGED: update set default ",(0,i.jsx)(n.code,{children:"desync"})," mode to 'off'."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: simplify package handling."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1183---2025-11-25",children:"[1.18.3] - 2025-11-25"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"Domains"})," table not updating with new packets due to React state reference issue."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"Domains"})," table columns layout being cramped with fixed widths."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1182---2025-11-25",children:"[1.18.2] - 2025-11-25"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"ClientHello Mutation"})," support for ",(0,i.jsx)(n.code,{children:"IPv6"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: Discovery presets missing default values for ",(0,i.jsx)(n.code,{children:"SNI Mutation"}),", ",(0,i.jsx)(n.code,{children:"TCP Window"}),", and ",(0,i.jsx)(n.code,{children:"Desync"})," settings."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1181---2025-11-25",children:"[1.18.1] - 2025-11-25"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: Unable to change ",(0,i.jsx)(n.code,{children:"ClientHello Mutation"})," mode in the Faking settings."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1180---2025-11-24",children:"[1.18.0] - 2025-11-24"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"IMPROVED: Overall performance (frontend and backend)."}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"TCP Window Manipulation"})," (",(0,i.jsx)(n.code,{children:"--tcp-win-mode"}),") - sends fake packets with manipulated TCP window sizes to confuse stateful DPI. Modes: ",(0,i.jsx)(n.code,{children:"oscillate"})," (cycling window values), ",(0,i.jsx)(n.code,{children:"zero"})," (zero-window probe attack), ",(0,i.jsx)(n.code,{children:"random"})," (randomized windows), ",(0,i.jsx)(n.code,{children:"escalate"})," (gradually increasing windows)."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"TCP Desync Attack"})," (",(0,i.jsx)(n.code,{children:"--tcp-desync-mode"}),") - injects fake TCP control packets (RST/FIN/ACK) with low TTL and corrupted checksums to desynchronize DPI connection tracking. Modes: ",(0,i.jsx)(n.code,{children:"rst"}),", ",(0,i.jsx)(n.code,{children:"fin"}),", ",(0,i.jsx)(n.code,{children:"ack"}),", ",(0,i.jsx)(n.code,{children:"combo"}),", ",(0,i.jsx)(n.code,{children:"full"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"SNI Mutation"})," for ClientHello fingerprint evasion - modifies TLS handshake structure to bypass DPI fingerprinting. Modes: ",(0,i.jsx)(n.code,{children:"duplicate"})," (inject fake SNIs), ",(0,i.jsx)(n.code,{children:"grease"})," (add GREASE extensions), ",(0,i.jsx)(n.code,{children:"padding"})," (add padding extension), ",(0,i.jsx)(n.code,{children:"reorder"})," (shuffle extensions), ",(0,i.jsx)(n.code,{children:"full"})," (all mutations combined), ",(0,i.jsx)(n.code,{children:"advanced"})," (TLS 1.3 features like PSK/key_share)."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1171---2025-11-23",children:"[1.17.1] - 2025-11-23"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"Out-of-Band"})," (OOB) data handling with configurable position, reverse order, and character (",(0,i.jsx)(n.code,{children:"--frag=oob"}),")."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"Out-of-Band"})," (OOB) strategies to ",(0,i.jsx)(n.code,{children:"B4Discovery"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"TLS Record Splitting"})," fragmentation strategy (",(0,i.jsx)(n.code,{children:"--frag=tls"}),") - splits ClientHello into multiple TLS records to bypass DPI expecting single-record handshakes."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"SACK dropping"})," (",(0,i.jsx)(n.code,{children:"--tcp-drop-sack"}),") - strips Selective Acknowledgment options from TCP headers to force full retransmissions and confuse stateful DPI tracking."]}),"\n",(0,i.jsxs)(n.li,{children:["UPDATED: Fake ",(0,i.jsx)(n.code,{children:"SNI"})," payload now uses TLS 1.3 ClientHello structure with ",(0,i.jsx)(n.code,{children:"staticcdn.duckduckgo.com"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.code,{children:"SNI"})," fragmentation for long domains (>30 bytes). Now splits 12 bytes before SNI end instead of middle, ensuring domain suffixes like ",(0,i.jsx)(n.code,{children:"googlevideo.com"})," are properly fragmented across packets."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.code,{children:"Matcher"})," performance with LRU caching for large geosite/geoip categories (70-90% CPU reduction for sets with big data inside)."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: ",(0,i.jsx)(n.code,{children:"Geodat"})," download workflow - files now immediately available in sets manager without restart, config auto-reloads after download."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: Set ",(0,i.jsx)(n.code,{children:"Fragmentation"})," tab refactored."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Logs level can be switched witout reloading the app."}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: Config validation bug where Main Set was compared against itself, causing startup failure with ",(0,i.jsx)(n.code,{children:"TCP ConnBytesLimit greater than main set"})," error."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: update default fake SNI payload to use new format."}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: Renamed ",(0,i.jsx)(n.code,{children:"--frag-sni-reverse"})," to ",(0,i.jsx)(n.code,{children:"--frag-reverse"})," and update related configurations."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1161---2025-11-20",children:"[1.16.1] - 2025-11-20"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"ADDED: Asynchronous packet injection for TCP and UDP traffic. Verdict is now sent to kernel immediately, with packet manipulation performed in parallel. Eliminates kernel queue blocking that previously caused video streaming hangs and site loading delays."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Critical performance bottleneck where each QUIC/UDP packet with default configuration (FakeSeqLength: 6) would block the kernel for 6ms minimum. This caused YouTube and other video services to buffer or hang intermittently."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: IPv6 QUIC packet processing incorrectly used TCP delay settings instead of UDP delay settings."}),"\n",(0,i.jsx)(n.li,{children:'FIXED: New configuration sets created from the Domains page were not saving custom names, defaulting to generic "Set 1/2/3" names instead.'}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: Removed unnecessary ",(0,i.jsx)(n.code,{children:"1ms"})," sleep delays when ",(0,i.jsx)(n.code,{children:"Seg2Delay"})," is set to ",(0,i.jsx)(n.code,{children:"0"}),", reducing packet processing latency by up to ",(0,i.jsx)(n.code,{children:"6ms"})," per QUIC packet."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1160---2025-11-17",children:"[1.16.0] - 2025-11-17"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"ADDED: Configuration sets can now be enabled/disabled without deletion."}),"\n",(0,i.jsx)(n.li,{children:"ADDED: Clear button next to the IP/CIDR list in the set configuration."}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Download ",(0,i.jsx)(n.code,{children:"GeoSite"}),"/",(0,i.jsx)(n.code,{children:"GeoIP"})," database files directly from Settings UI with preset sources."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: Redesigned ",(0,i.jsx)(n.code,{children:"/test"})," page UX - domains are now managed directly on the test page."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: Refactor ",(0,i.jsx)(n.code,{children:"Discovery"})," presets generation logic and add new test strategies."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: Resolved severe performance bottleneck on ",(0,i.jsx)(n.code,{children:"/domains"})," page when adding ASN filters (caused by expensive ASN lookup operations executing on every render)."]}),"\n",(0,i.jsxs)(n.li,{children:["REMOVED: Test domain configuration from ",(0,i.jsx)(n.code,{children:"Settings"})," - domains are now managed exclusively on the Test page."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1150---2025-11-16",children:"[1.15.0] - 2025-11-16"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: SYN fake packet functionality for advanced DPI bypass. Sends fake SYN packets with configurable payload length to confuse DPI systems before the real connection is established. Configure via ",(0,i.jsx)(n.code,{children:"--tcp-syn-fake"})," and ",(0,i.jsx)(n.code,{children:"--tcp-syn-fake-len"})," flags, or through the TCP settings in Web UI."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: IP information enrichment via ",(0,i.jsx)(n.code,{children:"IPInfo"})," API integration. When IPInfo token is configured in Settings \u2192 API, click on any destination IP in ",(0,i.jsx)(n.code,{children:"/domains"})," monitoring page to view detailed geolocation, ASN, organization, and network information."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"RIPE"})," ",(0,i.jsx)(n.a,{href:"https://www.ripe.net/",children:"Stat integration"})," for network intelligence. View ASN prefix announcements and detailed network information directly from the Web UI. Helps identify IP ranges for precise targeting."]}),"\n",(0,i.jsx)(n.li,{children:"ADDED: Configuration set import/export functionality. Share working bypass configurations between devices or users by exporting sets as JSON files. Import proven configurations with one click to quickly replicate successful setups across multiple installations."}),"\n",(0,i.jsx)(n.li,{children:"IMPROVED: Discovery test results now include individual configuration cards per domain instead of single recommended configuration, making it easier to analyze which specific settings work best for each target domain."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1140---2025-11-13",children:"[1.14.0] - 2025-11-13"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: Select target configuration set when adding domains or IP/CIDR addresses from ",(0,i.jsx)(n.code,{children:"/domains"})," monitoring page. Allows precise control over which configuration set receives the new entry."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: One-click configuration adoption from ",(0,i.jsx)(n.code,{children:"Discovery"})," test results. Apply the best-performing configuration directly to your configuration list without manual copying."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: Complete overhaul of ",(0,i.jsx)(n.code,{children:"Discovery"})," testing service with improved reliability and performance. Now they should work as expected."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Memory leaks and overall memory management improvements for better long-term stability."}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Update process through the Web UI."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1130---2025-11-10",children:"[1.13.0] - 2025-11-10"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: Click on destination IP addresses in ",(0,i.jsx)(n.code,{children:"/domains"})," monitoring page to add them to configuration. Modal allows adding either exact IP or CIDR notation for broader site coverage. This does not require to reload or restart B4, works on the fly."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Toggle switch in ",(0,i.jsx)(n.code,{children:"/domains"})," monitoring page to view all packets or only those with identified SNI/domain. Useful for monitoring and debugging ",(0,i.jsx)(n.code,{children:"UDP"})," traffic."]}),"\n",(0,i.jsx)(n.li,{children:"ADDED: Geodat domain/IP counters at configuration sets."}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: a new tab under ",(0,i.jsx)(n.code,{children:"/test"})," menu. ",(0,i.jsx)(n.code,{children:"Discovery"})," test results now show individual configuration cards per domain instead of a single recommended configuration, making it easier to see what works best for each specific domain."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.code,{children:"UDP"}),' port filtering now uses a single flexible field instead of separate "from" and "to" fields. Supports comma-separated ports and ranges (e.g., ',(0,i.jsx)(n.code,{children:"80,443,2000-3000"}),")."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: Packages count badge in ",(0,i.jsx)(n.code,{children:"/domains"})," menu now only counts packets processed by B4 targets."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: Replaced ",(0,i.jsx)(n.code,{children:"--udp-dport-min"})," and ",(0,i.jsx)(n.code,{children:"--udp-dport-max"})," flags with single ",(0,i.jsx)(n.code,{children:"--udp-dport-filter"})," flag for flexible port filtering."]}),"\n",(0,i.jsx)(n.li,{children:"CHANGED: Refactored UDP/QUIC packet handling and UDP-related UI tab in the set configuration."}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"UDP"})," entries are now logged even when UDP packets are configured to be ignored in the configuration"]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"UI"})," crash when using filter in /domains monitoring page."]}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Manually added domains no longer require service restart when geodat files are not configured."}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: Test Suites now correctly report success when DPI is bypassed regardless of ",(0,i.jsx)(n.code,{children:"HTTP status code"}),". Any HTTP response (including non-200 codes) indicates successful DPI circumvention."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1120---2025-11-09",children:"[1.12.0] - 2025-11-09"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: Configuration Sets - fine-grained bypass control for different targets","\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Create multiple configuration sets, each with independent TCP/UDP/fragmentation/faking settings"}),"\n",(0,i.jsx)(n.li,{children:"Target packets by SNI domain, destination IP/CIDR ranges, or UDP port ranges"}),"\n"]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"geoip.dat"})," support."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1110---2025-11-05",children:"[1.11.0] - 2025-11-05"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: DPI Bypass Test feature to verify that circumvention is working. The feature tests configured domains and measures download speeds to ensure B4 is functioning correctly. Visit the ",(0,i.jsx)(n.code,{children:"/test"})," page to run tests and ",(0,i.jsx)(n.code,{children:"/settings/checker"})," to configure test settings (define which domains to test, etc.)."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: New feature to reset B4 settings to their defaults. The reset button is located in the ",(0,i.jsx)(n.code,{children:"Core"})," tab on the ",(0,i.jsx)(n.code,{children:"Settings"})," page."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: Moved ",(0,i.jsx)(n.code,{children:"RESTART B4 BUTTON"})," to the ",(0,i.jsx)(n.code,{children:"Core"})," tab on the Settings page (under the ",(0,i.jsx)(n.code,{children:"Core Controls"})," section)."]}),"\n",(0,i.jsxs)(n.li,{children:["IMPROVED: Enhanced ",(0,i.jsx)(n.code,{children:"flowState"})," struct to track ",(0,i.jsx)(n.code,{children:"SNI"})," detection and processing status."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: Service restart functionality in the UI for different service managers (",(0,i.jsx)(n.code,{children:"Entware"}),"/",(0,i.jsx)(n.code,{children:"OpenWRT"}),"/",(0,i.jsx)(n.code,{children:"systemctl"}),")."]}),"\n",(0,i.jsxs)(n.li,{children:["FIXED: Pause shortcut (pressing down the ",(0,i.jsx)(n.code,{children:"P"})," key on the domains and logs pages) interfering with search input."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1101---2025-11-03",children:"[1.10.1] - 2025-11-03"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["IMPROVED: Intermittent connection failures where blocked sites would randomly fail to load in certain browsers (",(0,i.jsx)(n.code,{children:"Safari"}),", ",(0,i.jsx)(n.code,{children:"Firefox"}),", ",(0,i.jsx)(n.code,{children:"Chrome"}),"). Connections ",(0,i.jsx)(n.em,{children:"should"})," now be more stable and reliable across all browsers by optimizing packet fragmentation strategy."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"1100---2025-11-02",children:"[1.10.0] - 2025-11-02"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: Automatic ",(0,i.jsx)(n.code,{children:"iptables"}),"/",(0,i.jsx)(n.code,{children:"nftables"})," rules restoration. B4 now automatically detects this and restores itself without requiring a manual restart."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: New ",(0,i.jsx)(n.code,{children:"--tables-monitor-interval"})," setting to control how often B4 checks if its rules are still active (default: ",(0,i.jsx)(n.code,{children:"10"})," seconds). Set to ",(0,i.jsx)(n.code,{children:"0"})," to disable automatic monitoring."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"192---2025-11-02",children:"[1.9.2] - 2025-11-02"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"IMPROVED: Increase TTL and buffer limit for flow state management."}),"\n",(0,i.jsx)(n.li,{children:"IMPROVED: enhance SNI character validation."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"191---2025-11-02",children:"[1.9.1] - 2025-11-02"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: Return back missing ",(0,i.jsx)(n.code,{children:"geosite path"})," field to the settings."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"190---2025-11-02",children:"[1.9.0] - 2025-11-02"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: Hotkeys to the ",(0,i.jsx)(n.code,{children:"/domains"})," and ",(0,i.jsx)(n.code,{children:"/logs"})," page. Press ",(0,i.jsx)(n.code,{children:"ctrl+x"})," or ",(0,i.jsx)(n.code,{children:"Delete"})," keys to clear the entries. Press ",(0,i.jsx)(n.code,{children:"p"})," or ",(0,i.jsx)(n.code,{children:"Pause"})," to pause the stram."]}),"\n",(0,i.jsx)(n.li,{children:"ADDED: Parse regex entries from the geosite files."}),"\n",(0,i.jsx)(n.li,{children:"ADDED: Connection bytes limit configuration for TCP and UDP in network settings"}),"\n",(0,i.jsx)(n.li,{children:"FIXED: Wrong total number of total domains in the settings."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"180---2025-11-01",children:"[1.8.0] - 2025-11-01"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"nftables"})," support."]}),"\n",(0,i.jsxs)(n.li,{children:["CHANGED: ",(0,i.jsx)(n.code,{children:"--skip-iptables"})," and ",(0,i.jsx)(n.code,{children:"--clear-iptables"})," renamed to ",(0,i.jsx)(n.code,{children:"--skip-tables"})," and ",(0,i.jsx)(n.code,{children:"--clear-tables"}),"."]}),"\n",(0,i.jsx)(n.li,{children:"IMPROVED: TCP flow handling by fragmenting packets after SNI detection."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"170---2025-10-31",children:"[1.7.0] - 2025-10-31"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"ADDED: 'RESTART SERVICE` Button in the Settings to perform the B4 restart from the Web UI."}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: Add ",(0,i.jsx)(n.code,{children:"quiet"})," mode and ",(0,i.jsx)(n.code,{children:"geosite"})," source/destination options to installer script. Use ",(0,i.jsx)(n.code,{children:"b4install.sh --help"})," to get more information."]}),"\n",(0,i.jsx)(n.li,{children:"ADDED: Sort Domains by clicking the columns."}),"\n",(0,i.jsx)(n.li,{children:"ADDED: Update a new version from the Web Interface."}),"\n",(0,i.jsxs)(n.li,{children:["REMOVED: iptables ",(0,i.jsx)(n.code,{children:"OUTPUT"})," rule."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"160---2025-10-29",children:"[1.6.0] - 2025-10-29"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["FIXED: ",(0,i.jsx)(n.code,{children:"Dashboard"})," works again."]}),"\n",(0,i.jsxs)(n.li,{children:["REMOVED: ",(0,i.jsx)(n.code,{children:"--conntrack"})," and ",(0,i.jsx)(n.code,{children:"-gso"})," flags since they both are not used in the project."]}),"\n",(0,i.jsx)(n.li,{children:"IMPROVED: Installation script now handles a geosite file setup."}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"150---2025-10-28",children:"[1.5.0] - 2025-10-28"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"--clear-iptables"})," argument to perform a cleanup of iptable rules."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"IPv6"})," support."]}),"\n",(0,i.jsxs)(n.li,{children:["ADDED: ",(0,i.jsx)(n.code,{children:"--ipv4"})," (default is ",(0,i.jsx)(n.code,{children:"true"}),") and ",(0,i.jsx)(n.code,{children:"--ipv6"})," (default is ",(0,i.jsx)(n.code,{children:"false"}),") arguments to control protocol versions."]}),"\n",(0,i.jsx)(n.li,{children:"IMPROVED: Handling of geodata domains."}),"\n"]})]})}function h(e={}){const{wrapper:n}={...(0,r.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(c,{...e})}):c(e)}},416(e,n,t){t.d(n,{R:()=>o,x:()=>a});var s=t(9471);const i={},r=s.createContext(i);function o(e){const n=s.useContext(r);return s.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function a(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:o(e.components),s.createElement(r.Provider,{value:n},e.children)}}}]);