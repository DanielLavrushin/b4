---
sidebar_position: 8
title: Дискавери
---

# Дискавери

Дискавери подбирает стратегию обхода для сайтов, указанных в поле ввода, и превращает результат в [сет](./sets/). Запуск применяет встроенные пресеты стратегий один за другим на отдельной очереди захвата, запрашивает через каждый из них все сайты и оставляет те пресеты, с которыми страница загрузилась. Уже настроенные сеты во время запуска продолжают работать: через проверяемую стратегию идут только запросы самого запуска.

Базовый сценарий описан в разделе [Быстрый старт](./quickstart). Эта страница описывает, что делает запуск, как читать его результаты и что попадает в созданный сет.

## Ввод

Поле принимает домен или полный URL, несколько сразу через запятую или с новой строки. Домен запрашивается как `https://<домен>/`; URL запрашивается как есть, поэтому страницу по конкретному пути можно указать напрямую. Две записи с одним и тем же именем хоста считаются одним сайтом.

![Ввод и параметры Дискавери](/img/discovery/ru/20260905160100.png)

Параметры под полем меняют то, как запуск проверяет, а не то, что он ищет:

| Параметр | Действие |
| --- | --- |
| **Проверять подмену DNS** | Перед стратегиями каждый сайт разрешается через DNS-серверы из раздела Настройки, Дискавери, а ответы сравниваются с эталоном. Сайт, для которого резолвер отдаёт ложный адрес, получает в свой сет DNS-перенаправление. Выключенный параметр означает доверие системному резолверу. |
| **Сначала пробовать стратегии, которые уже срабатывали** | Пресеты, победившие в прошлых запусках, проверяются раньше встроенного списка. **Забыть их** очищает этот список. |
| **Подтверждать каждую стратегию** | Сколько запросов подряд пресет должен пройти во время поиска, от 1 до 5. Не связано с подтверждением в конце запуска, которое всегда запрашивает победителя ещё три раза. |
| **Версия TLS для проверок** | Ограничивает запросы TLS 1.2 или 1.3 и ограничивает созданный сет этой версией, поскольку часть DPI обрабатывает их по-разному. |
| **Версия IP для проверок** | То же для IPv4 и IPv6. Показывается только когда движок захвата обрабатывает обе. |
| **Захваченный ClientHello как содержимое фейков** | Поддельные пакеты несут ClientHello, захваченный в разделе Настройки, Пэйлоады, вместо встроенных payload. |

## Запуск

Запуск проходит шесть этапов, которые показываются шагами, пока он идёт:

1. **DNS**. Каждый сайт разрешается, ответы проверяются, если проверка DNS не выключена.
2. **Без обхода**. Каждый сайт запрашивается вообще без стратегии. Сайт, который загрузился здесь, получает вердикт «обход не нужен» и дальше в вердикте не участвует, хотя для сравнения его продолжают запрашивать через каждый пресет.
3. **Стратегии**. Запомненные победители, затем 18 встроенных стартовых пресетов применяются по одному, и через каждый запрашиваются все сайты.
4. **Подбор параметров**. Для каждого семейства техник, давшего рабочий пресет, параметры варьируются на сайте, который лучше всего на него откликнулся, а результат проверяется на всех сайтах.
5. **Комбинации**. Если сработали два семейства или больше, проверяются пресеты, которые их сочетают.
6. **Подтверждение**. Пресет-победитель каждого сайта собирается ровно в том виде, в каком он был бы сохранён как сет, и запрашивается ещё три раза. Победитель, не прошедший хотя бы один из этих запросов, уступает место следующему кандидату.

![Запуск в процессе](/img/discovery/ru/20260905160200.png)

Панель показывает текущий этап, прошедшее время, число проверенных конфигураций и последнюю строку журнала запуска. **Показать журнал** открывает журнал целиком. Число проверок на этапах подбора и комбинаций зависит от того, что нашли первые этапы, поэтому процента на панели нет. По строке на сайт видно, что найдено к этому моменту: стратегия с описанием в одно предложение, число перебранных пресетов или то, что сайт открывается без b4.

**Остановить и сохранить найденное** завершает запуск раньше. Всё найденное сохраняется и записывается в историю с пометкой «не подтверждена», поскольку этап подтверждения для него не выполнялся. Запуск, начатый [мониторингом доменов](./watchdog) для восстановления сета, показывается так же, но остановить его с этой страницы нельзя.

:::info
Одновременно может идти только один запуск. Мониторинг доменов использует ту же среду, а обновление файрвола, начатое во время запуска, ждёт его завершения. После окончания запуска очередь и правила, которые он использовал, освобождаются, на что уходит несколько секунд; страница сообщает об этом и включает **Начать**, когда всё готово.
:::

## Результаты

Каждый сайт из запуска получает один вердикт:

| Вердикт | Значение |
| --- | --- |
| **Стратегия найдена** | Хотя бы один пресет открыл сайт. Карточка называет победителя, описывает его одним предложением и перечисляет, что сделает сет. Бейдж говорит, прошло ли подтверждение. |
| **Обход не нужен** | Сайт открылся без стратегии. Применять нечего: сет для него лишь прогонит трафик через ненужные изменения пакетов. |
| **Адрес заблокирован** | Соединения со всеми известными адресами сайта обрываются раньше, чем начинается TLS, поэтому стратегия на уровне пакетов не поможет. Такому сайту нужен сет с маршрутизацией через [прокси или VPN](./sets/routing). |
| **Ничего не найдено** | Все пресеты не сработали. В журнале видно, как именно не прошёл каждый. Повторный запуск с TLS 1.2 или захваченным ClientHello иногда меняет исход. |

Сайты с одним и тем же пресетом-победителем показываются на одной карточке, поскольку применение создаёт один сет для всех них. Карточка строится по самому сету, с теми же строками, что и на странице сетов: стратегия разделения с параметрами, поддельные пакеты, цели и наличие DNS-перенаправления. Ниже **Подробности** перечисляют остальные сработавшие пресеты, каждый с кнопкой **Взять вместо неё**, число подтверждений и скорость по каждому сайту, а также сколько из проверенных конфигураций открыли страницы.

![Результаты запуска](/img/discovery/ru/20260905160300.png)

## Применение

**Применить как сет** открывает диалог с построчным превью сета. Название по умолчанию совпадает с первым сайтом, с суффиксом, если запуск был ограничен версией TLS или IP. Для одного сайта совпадение можно сократить до родительского домена, чтобы сет покрывал и поддомены. Если сайт уже числится в другом включённом сете, диалог сообщает об этом: домен из двух включённых сетов обрабатывает тот, что стоит раньше в списке, поэтому сайт переносится в новый сет. Если включённый сет уже использует ту же стратегию, сайты можно добавить в него вместо создания нового.

Созданный сет попадает в начало списка и сразу включён. Его цели - названные сайты плюс категории geosite или geoip, которые b4 связывает с ними по встроенной таблице CDN, если соответствующая база установлена. Адреса, узнанные во время запуска, в сет не переносятся, поэтому сет совпадает по имени сервера. DNS-перенаправление включается только для сайта, резолвер которого отдавал ложный адрес.

:::tip
Запись истории хранит сет, собранный запуском, поэтому результат можно применить позже, не запуская Дискавери заново. Состояние подтверждения сохраняется вместе с ним.
:::

## История

Хранятся последние сто результатов, по одной записи на сайт; новый запуск для того же сайта заменяет старую запись. Каждая запись показывает вердикт, стратегию словами вместе с именем пресета и время запуска. **Применить** устанавливает сет, собранный запуском для этого сайта; два сайта из одного запуска с общим пресетом-победителем делят один сет и применяются вместе. **Повторить** начинает новый запуск для сайта; запись можно удалить отдельно или очистить всю историю.

![История Дискавери](/img/discovery/ru/20260905160400.png)

:::info
[Мониторинг доменов](./watchdog) запускает Дискавери сам, когда отслеживаемый домен перестаёт открываться, и применяет результат к сету, который его обрабатывает.
:::
